# Задача #117 — уровень риска не различает задачи: признаки не о том

**Стадия:** готово к мерджу
**Ветка:** `feat/117-work-breadth-signal`
**PR:** [#121](https://github.com/k-kuzmin/domovoy/pull/121), черновик снят
**Следующий шаг:** мердж — открытых замечаний нет, 13 чеков зелёные, обе половины ревью `approve`. Порог ширины решён владельцем: остаётся 3. Оговорка та же: круг 2 корректности одобрял дерево до трёх кругов починки, и поверх него легли правка `scripts/risk-score.sh` (круг 2) и две правки прозы (круги 2 и 3) — машинным ревью они не смотрены, машинные круги исчерпаны (2 из 2 по потолку `medium`)

- [x] разбор — границы ясны, вопросы заданы
- [x] план — согласован владельцем (метка `plan/approved`)
- [x] реализация
- [x] ревью — обе половины чисты: безопасность `approve` кругом 1, корректность `approve` кругом 2
- [x] починка — круги 1, 2 и 3

## Границы

**Входит:**

- `pipeline/risk.json` — набор признаков: новые сигналы, изменённые пороги
  существующих, `note` каждого изменённого;
- `scripts/risk-score.sh` — вычисление выбранных признаков в обоих режимах и
  подкоманда замера по корпусу, если нынешней `history` для замера доли не
  хватает;
- `scripts/risk-score.test.sh` — сценарии на выбранные признаки, у каждого
  положительная и отрицательная половина;
- `scripts/fixtures/plans/**` — фикстуры контрольной пары, если план #84 и
  работа масштаба #110 иначе невоспроизводимы;
- `docs/tasks/117.md` — числа замеров и отвергнутые кандидаты;
- `docs/decisions/**` — запись, если выбранный признак расходится с ТЗ фазы 2.

**Не входит:**

- покрытие новых предметных зон (`allowanonymous`, каталог эндпоинтов) — #115;
- пол уровня `low` в `pipeline/route.json`: сколько кругов полагается
  уровню — запись 0020, отдельный вопрос;
- числовые уровни существующих сигналов (`high` против `medium`) — #103;
- автоматическая подстройка порогов по метрикам — запрещена `pipeline/risk.json`.

## Вопросы и ответы

| Вопрос | Ответ | Кто ответил |
|---|---|---|
| | | |

## Решения

**Разбор сверен оркестратором, а не принят от шага.** Вердикт `step-triage`
пришёл с `ready: true`, но состав работы в нём пересказан по **кейсу** из тела
задачи — плану #84 (`.claude/agents/issue-scout.md`, `docs/decisions/0021`,
`scripts/rules-sync.sh`), а не по предмету #117. Кейс — это то, что уровень не
различил; предмет — признаки, которыми уровень считается. Границы выше
восстановлены по телу задачи оркестратором.

**Что проверено до плана, чтобы план не проверял это заново.**

- подкоманда `history` в `scripts/risk-score.sh` существует
  (`scripts/risk-score.sh:82`, ветка `history` на строке 702) — критерий
  «доля корпуса замерена» опирается на существующую команду, строить харнесс
  замера с нуля не нужно;
- машиночитаемых планов в дереве **три** — `scripts/fixtures/plans/{81,83,93}.json`.
  Кандидаты `action: "create"` и `tests[].new` читаются только из плана в форме
  `plan-schema.json`, и корпуса под них нет: замерить их долю по 39 PR нечем.
  Кандидаты «число зон» и «число файлов» считаются из путей и меряются по всему
  корпусу.

## Уровень риска и маршрут

Прогон по плану круга 1:

```
bash scripts/risk-score.sh plan <план>
Уровень: medium
Факты: файлов в плане 8
Сигналы:
  [medium] app-configuration — конфигурация приложения и её примеры
      путь: pipeline/risk.json
```

`bash scripts/route.sh level medium` — сокращений ни одной позиции: машинный
круг ревью плана обязателен до апрува владельца, потолок два круга; ревью PR —
до двух кругов.

Отдельно стоит записи: `medium` этот план получил **не** за предмет работы, а за
единственный путь `pipeline/risk.json`, попадающий в `app-configuration`. Тот же
план без правки конфига дал бы `low` — ровно то, о чём задача.

## Круги плана

**Круг 1** — уровень `medium`, машинное ревью корректности дало
`changes_requested`: один blocker, два major, три nit.

Blocker по существу: план мерил суммарную долю `medium+` по корпусу, а критерий
задачи говорит о доле срабатывания **самого признака** (38/39 и 28/39 в теле
задачи). Базовая доля `medium+` — 9 из 39, поэтому правило плана отвергало бы
признак примерно на одиннадцати добавленных PR, а критерий — на двадцати.

**Факт одного замечания сверен и не подтвердился.** Nit 4 утверждал, что у
PR #112 `baseRefOid` и первый родитель коммита мержа — разные деревья. Сверено:
`baseRefOid = 4d6b3c25…`, родители мержа `e57dae33` — `4d6b3c2 0432ba8`, первый
родитель и есть база. Ловушка из `scripts/fixtures/plans/README.md:15-20` — про
PR #89, здесь её нет. Само замечание оставлено в силе по другой причине: способ
приёмки должен быть командой, а не пересказом «коммит мержа и его родитель».

**Круг 2** — все замечания закрыты, уровень прежний `medium`. Сверх замечаний
план нашёл своё: новый `kind` без собственной ветки в `evaluate()` прошёл бы
проверку конфига и провалился в общий `else` с пустыми `paths`, `plan_flags` и
`keywords` — сигнал молча не срабатывал бы никогда
(`scripts/risk-score.sh:520-536`, сверено оркестратором).

Сверено там же: цикла по каталогу фикстур в `scripts/plan.test.sh` нет —
`FIXTURES` на строках 48, 612, 636, вызовы `check_historical 93/83/81`. Значит
`84.json` этот харнесс не роняет и пина `--at` не требует.

Машинных кругов плана израсходовано два из двух — потолок уровня `medium`.

**Контрольная пара проверена на подмену доказательства.** Опасение: план #84
мог бы получить `medium` не за ширину, а за существующий сигнал, и пара
доказывала бы не то. Сверено по финальному плану #84 (круг 3): десять файлов —
`.claude/agents/issue-scout.md`, `.claude/CLAUDE.md`, `docs/decisions/0021-…`,
`docs/decisions/README.md`, `docs/pipeline.md`, `docs/rules/plan.md`,
`docs/rules/README.md`, `docs/tasks/84.md`, `scripts/rules-sync.sh`,
`scripts/rules-sync.test.sh`. Ни одного пути `pipeline/**`, то есть ни одного
существующего сигнала: сегодня этот план и даёт `low` с нулём сигналов. Любой
`medium` после правки приходит только от `work-breadth`.

**Цикл фикстур `84.json` не роняет.** `scripts/risk-score.test.sh:869-888`
требует от каждой фикстуры кода 0 и первой строки `Уровень: (low|medium|high)`;
конкретного уровня он не утверждает, правки этого сценария не нужно.

## Находка на стороне: машиночитаемая форма плана теряется в локальном режиме

Найдено по ходу задачи, к её предмету не относится — записано, чтобы не искать
заново.

Вопрос был «почему машиночитаемых планов всего три». Ответ: их не три, их ноль.
Три файла в `scripts/fixtures/plans/` — перенесённый вручную тестовый корпус,
заведённый `7b8d93c` от 30.08.2026, за день **до** перехода на машиночитаемый
план (PR #99, `fb88374`, 31.08.2026). Это фикстуры валидатора, а не архив
планов.

Форма плана — промежуточная по устройству: JSON → `validate` → `render` →
публикация markdown. Но **два потребителя расходятся**:

- **агентский цикл** кладёт JSON в комментарий свёрнутым блоком —
  `.github/workflows/agent-plan.yml:230-238`, `<details><summary>Машиночитаемая
  форма плана</summary>`;
- **локальный оркестратор** публикует только рендер: `.claude/CLAUDE.md`,
  пункт «публикует наружу», называет порядок `validate` → `render` →
  публикация и свёрнутого блока не упоминает.

С 31.08.2026 смержено восемь PR (#101, #104, #107, #108, #111, #112, #114,
#118), все локальным маршрутом. JSON ни одного из них не сохранился нигде.

**Цена видна прямо в этой задаче.** Фикстура `scripts/fixtures/plans/84.json`
переносится руками из комментария именно потому, что JSON плана #84 не пережил
публикацию, — и риск «перенос может разойтись с опубликованным планом» в плане
#117 существует только из-за этого расхождения. Круг 1 ревью плана начал отчёт
словами «машиночитаемого свёрнутого блока в комментарии нет»: шаг искал блок,
потому что цикл его кладёт.

Круг 2 плана #117 после находки дополнен свёрнутым блоком — комментарий
отредактирован, форма этого плана сохранена.

Заведена задачей **#120**. Выжимка `issue-scout` рекомендовала «дописать в
#117» — отклонено: план #117 согласован, реализация идёт, `.claude/CLAUDE.md`
стоит у него в «Что не входит», и место с классом у находки другие. Оценка
`мелочь` тоже отклонена: за находкой развилка «блок в комментарии против файла
в ветке», а не правка одной строки.

## Реализация

### Что выбрано признаком

**Ширина работы — число различных подсистем среди изменённых путей.** Признак
считается из путей и потому виден обоим входам: и плану до первой строки кода,
и готовому диффу. Это единственный признак объёма с такой симметрией — размер в
режиме `plan` не измеряется вовсе (`risk-score.sh`, ветка `kind=size` проверяет
`FACT_KIND`), и это пробел #115, а не порог.

Единица счёта названа подсистемой, а не зоной: слово «зона» в конфиге занято
предметными зонами сигналов. Список `subsystems` упорядочен, выигрывает первое
совпадение, замыкающая запись «прочее» с шаблоном `**` ловит остаток — её
отсутствие при включённом сигнале ширины роняет скрипт кодом 2.

### Порог: два прохода, 07.09.2026

Корпус — **40** смерженных PR (в теле задачи было 39: корпус вырос).

**Проход 1**, `breadth_subsystems` = «нет». Движок печатает, что ширину не
считал; распределение из вывода:

```
  Строк изменено на PR: p50 = 327, p75 = 898, p90 = 2251, максимум = 3568
  Файлов на PR: p50 = 3, p75 = 10, p90 = 13, максимум = 26
  Подсистем на PR: p50 = 2, p75 = 5, p90 = 7, максимум = 8
  Порог в конфиге сейчас: строк 1538, файлов 11, подсистем нет
```

Число ушло в конфиг руками. **Проход 2**, `breadth_subsystems` = 3:

```
Доля срабатывания по сигналам
  sensitive-area — сработал на 0 из 40 PR (0 %)
  db-migration — сработал на 0 из 40 PR (0 %)
  api-contract — сработал на 0 из 40 PR (0 %)
  di-composition — сработал на 0 из 40 PR (0 %)
  app-configuration — сработал на 5 из 40 PR (12 %)
  platform-code — сработал на 0 из 40 PR (0 %)
  new-dependency — сработал на 0 из 40 PR (0 %)
  diff-size — сработал на 7 из 40 PR (18 %)
  work-breadth — сработал на 16 из 40 PR (40 %)

Распределение уровней по корпусу
  high: 0 из 40 (0 %)
  medium: 19 из 40 (48 %)
  low: 21 из 40 (52 %)
  medium и выше: 19 из 40 (48 %)
```

До появления признака `medium` и выше получали **10 из 40 (25 %)**, `low` —
30 из 40. Стало 19 против 21: уровень перестал быть состоянием по умолчанию.

**Почему 3, а не 2 — расхождение с буквой плана, названное числами.** План
задавал правило выбора так: наименьшее число подсистем, при котором
`work-breadth` срабатывает менее чем на половине корпуса **по своей собственной
доле**, работа масштаба #110 остаётся `low`, а план #84 поднимается. По этому
правилу выходит **2**, и правило выполняется при обоих кандидатах:

| Порог | Сигнал сработал | Сам поднял с `low` | `medium` и выше по корпусу |
|---|---|---|---|
| 2 | 18 из 40 (45 %) | 11 из 40 (27 %) | 21 из 40 (52 %) |
| **3** | **16 из 40 (40 %)** | **9 из 40 (22 %)** | **19 из 40 (48 %)** |

Взят 3, и это суждение, а не вычисление. **Первая ошибка этого рассуждения
поймана и исправлена:** первая редакция журнала и записи 0022 объявляла порог 2
нарушающим критерий задачи — «признак, поднимающий больше половины корпуса,
отвергается». Это неверно. 21 из 40 — суммарная доля корпуса, включающая PR,
поднятые `diff-size` и `app-configuration`; сам признак при пороге 2 поднимает
11 из 40, то есть 27 %. Мерить кандидата суммарной долей — ровно та мера,
которую снял blocker 1 круга 1 ревью плана, и возвращать её нельзя.

Настоящие причины взять 3 такие. Доля срабатывания 45 % стоит вплотную к
границе отказа: корпус растёт, и порог, живущий в одном проценте от неё,
пришлось бы пересматривать на ближайшем прогоне. И вторая, важнее: при пороге 2
`medium` и выше получает больше половины корпуса — это меняет уже не оценку
признака, а смысл уровня для `pipeline/route.json`, а такой вопрос решает
владелец, а не шаг реализации.

Цена смены — один символ в `thresholds.breadth_subsystems`. Обе величины
печатаются командой, обе стоят числами здесь, в `note` и в записи 0022; выбор
называется в теле PR отдельным абзацем.

Есть и третье соображение, но оно вторично и решающим не считается: при пороге
2 существующий сценарий харнесса «DI-композиция и конфигурация приложения дают
medium, правка скрипта пайплайна не даёт ничего» (`scripts/risk-score.test.sh`)
перестал бы быть верным по названию — три подсистемы там дали бы `medium`. Это
была бы смена утверждения, а не пересчёт ожидания.

### Контрольная пара — оба прогона целиком

```
$ bash scripts/risk-score.sh plan scripts/fixtures/plans/84.json
Уровень: medium
Режим: plan scripts/fixtures/plans/84.json
Конфиг: /d/Portfolio/Projects/home-agent/pipeline/risk.json
Факты: файлов в плане 10, подсистем 7 (.claude, .claude/agents, scripts, docs/rules, docs, docs/decisions, docs/tasks)
Сигналы:
  [medium] work-breadth — ширина работы: затронуто подсистем больше порога
      ширина работы: подсистем 7 (порог 3): .claude, .claude/agents, scripts, docs/rules, docs, docs/decisions, docs/tasks
Сигналы вне счёта:
  [выключен] coverage-drop — падение покрытия или mutation score: Выключен: включается в PR фазы 3, вместе с мутационным тестированием. Источника метрики сейчас нет, и сигнал, считающий её отсутствие нулём, давал бы medium каждому PR.
Не измерялось в режиме plan: размер диффа и ключевые слова по добавленным строкам — диффа ещё нет.
```

```
$ bash scripts/risk-score.sh diff e57dae3^ e57dae3
Уровень: low
Режим: diff e57dae3^..e57dae3 (репозиторий: /d/Portfolio/Projects/home-agent)
Конфиг: /d/Portfolio/Projects/home-agent/pipeline/risk.json
Факты: файлов 2, строк изменено 120, подсистем 2 (.claude/agents, docs/tasks)
Сигналы: ни один не сработал
Сигналы вне счёта:
  [выключен] coverage-drop — падение покрытия или mutation score: Выключен: включается в PR фазы 3, вместе с мутационным тестированием. Источника метрики сейчас нет, и сигнал, считающий её отсутствие нулём, давал бы medium каждому PR.
```

Пара разведена, и разводит её ровно новый признак: в плане #84 нет ни одного
пути `pipeline/**`, то есть ни одного существующего сигнала (сверено при
планировании, круг 2).

**Расхождение с шестёркой из тела задачи.** Там у плана #84 названо шесть зон,
здесь семь. Причина: перечень в теле задачи короче, и `docs/pipeline.md` не
попадал в него никуда. Число не подогнано — расхождение записано в `note`
сигнала.

### Отвергнутые кандидаты — с числами

| Кандидат | Число | Почему отвергнут |
|---|---|---|
| Сигнал по флагу плана `allow_protected` | 38 PR из 39 (замер в теле задачи) | признак, верный в 97 % случаев, делает уровень константой |
| Сигнал по путям исполняемой обвязки (`scripts/`, `.github/workflows/`, `.githooks/`) | 28 из 39 (замер в теле задачи) | те же 72 %: здесь предмет работы и есть обвязка |
| Число создаваемых файлов (`action: "create"`) | машиночитаемых планов в дереве 3 против 40 PR корпуса | корпуса нет — долю замерить нечем; и признак, считающийся только из плана, ломает сценарий «Оба входа на одних и тех же фактах дают один уровень» |
| Число новых тестов (`tests[].new`) | там же — 3 плана против 40 PR | то же самое, обе причины |
| Пересчёт порога `diff_files` по корпусу вместо нового признака | при `diff_files` = 3 (p50 корпуса) `diff-size` срабатывает на **20 PR из 40 (50 %)**, `medium` и выше получают 23 из 40 (58 %) | ровно половина корпуса по самому сигналу и больше половины по уровню — правило отказа выполняется числом. Плюс главное: в режиме `plan` размер не считается вовсе, и кейс задачи — план #84 — новый порог не поймал бы ни при каком значении |
| Порог ширины 2 вместо 3 | сигнал 18 из 40, `medium` и выше 21 из 40 | больше половины корпуса по уровню — см. выше |
| Пришить ширину третьим условием к `diff-size` | цитата `pipeline/risk.json`: «В режиме plan не измеряется — диффа ещё нет» | ширина унаследовала бы это молчание там, где она и нужна, а `note` стал бы ложным |
| Мерить кандидата суммарной долей `medium+`, а не долей самого сигнала | базовая доля `medium+` — 10 из 40 | суммарная включает PR, поднятые другими сигналами; по ней нельзя решить, работает ли новый признак |
| Считать подсистемы как «сколько шаблонов `paths` сигналов совпало» | — | пересекающиеся глобы засчитали бы файл дважды, порядок был бы неявным |
| Не считать подсистемой `docs/tasks/**` | — | спецслучай ради красоты чисел: журнал трогает почти каждый PR, это постоянное +1 у всех, ранга работ не меняет |
| Частичный план в `scripts/fixtures/plans/84.json` | — | шапка каталога называет его файлы экземплярами схемы; частичный файл сделал бы её ложной второй раз |

Замер отвергнутого `diff_files` шёл прогоном `bash scripts/risk-score.sh
history --config` на копии таблицы **вне дерева**; отслеживаемый
`pipeline/risk.json` эта задача по порогам размера не трогает.

### Найденное сверх плана

**Перечень подсистем печатается рядом с числом.** «Подсистем 7» без перечня —
число, которое нечем проверить: разъехавшийся список подсистем виден только по
итогу. Строка «Факты» печатает и число, и перечень, а таблица `history`
получила столбец «Подсистем» — без него доля срабатывания не сверяется с
корпусом ничем, кроме доверия.

**Проверка списка подсистем живёт в том же единственном запросе `jq`, что и
проверка сигналов.** Второй вызов стоил бы процесса на каждый коммит корпуса —
и, что важнее, не ловился бы сценарием «Отказ разбора конфига роняет скрипт»:
подставной `jq` в харнессе отказывает ровно на программе со словом `$required`.

## Изменённые файлы

| Файл | Зачем |
|---|---|
| `pipeline/risk.json` | список `subsystems`, порог `thresholds.breadth_subsystems` = 3, сигнал `work-breadth`, происхождение порога в `thresholds.source` |
| `scripts/risk-score.sh` | вид `breadth` в проверке конфига и своя ветка в `evaluate()`; счёт подсистем в обоих сборщиках; строка «Факты» с числом и перечнем; строка о несчитанном пороге в обоих режимах; три новых блока и столбец в `history`; шапка |
| `scripts/risk-score.test.sh` | шесть новых сценариев |
| `scripts/fixtures/plans/84.json` | высокая половина контрольной пары |
| `scripts/fixtures/plans/README.md` | счёт «три» → «четыре», два потребителя каталога вместо одного, строка про `84.json` |
| `docs/decisions/0022-work-breadth-as-risk-signal.md` | расхождение с закрытой таблицей сигналов ТЗ фазы 2 |
| `docs/decisions/README.md` | строка 0022 в индексе |
| `docs/tasks/117.md` | журнал задачи |

## Проверено

| Пункт контракта | Что запускалось | Исход |
|---|---|---|
| **pair** | `bash scripts/risk-score.sh plan scripts/fixtures/plans/84.json` и `bash scripts/risk-score.sh diff e57dae3^ e57dae3` | `medium` против `low`; вывод обоих прогонов целиком выше. В первом — сигнал `work-breadth` с числом подсистем и порогом, во втором — «Сигналы: ни один не сработал» |
| **corpus-share** | `bash scripts/risk-score.sh history --base main`, два прохода, 07.09.2026 | напечатаны три новые строки: доля по каждому сигналу, распределение подсистем, распределение уровней. `work-breadth` — 16 из 40 (40 %), `medium` и выше — 19 из 40 (48 %). Обе доли меньше половины, признак не отвергнут |
| **rejected-numbers** | таблица «Отвергнутые кандидаты» выше; замер `diff_files` — `history --config` на копии вне дерева | у каждого кандидата число или цитата. Обратная сторона: в `pipeline/risk.json` не попал ни один отвергнутый признак — сигнал добавлен ровно один |
| **scenarios** | `bash scripts/risk-score.test.sh` | до правки «Пройдено: 32, провалено: 0, пропущено: 0», после — «Пройдено: 38, провалено: 0, пропущено: 0». Ровно на шесть больше, и это шесть новых сценариев ниже |
| — | `bash scripts/wiring.sh`, `bash scripts/rules-sync.sh`, `bash scripts/test-count-invariant.sh`, `bash scripts/plan.test.sh` | сверка обвязки и сверка правил — код 0; инвариант числа тестов — 42 против снимка 42. `bash scripts/plan.test.sh` — «Пройдено: 30, провалено: 0, пропущено: 0»: утверждение шапки каталога фикстур проверено прогоном, а не только чтением — `84.json` харнесс валидатора не роняет и пина `--at` не требует. Механическая опора та же, что при планировании: `FIXTURES` встречается на строках 48, 612, 636, цикла по каталогу нет, планы перечислены поимённо `check_historical 93/83/81` |
| **note** | чтение `pipeline/risk.json` | `note` сигнала `work-breadth` называет факт (число подсистем по списку, первое совпадение выигрывает), происхождение порога с датой и корпусом, числа контрольной пары и расхождение с шестёркой из тела задачи; `thresholds.source` называет ту же дату и тот же корпус |

Шесть новых сценариев `scripts/risk-score.test.sh`:

1. Ширина работы: план #84 из фикстуры выше low, работа масштаба #110 остаётся low
2. Подсистем выше порога — сигнал ширины в обоих режимах, ровно порог — молчание
3. Нечисловой порог подсистем и сломанный список подсистем роняют скрипт кодом 2
4. Порог подсистем не задан — код 0, уровень low и строка о несчитанной ширине в обоих режимах
5. Каждый путь дерева попадает ровно в одну подсистему списка
6. history печатает долю срабатывания по каждому сигналу, распределение подсистем и распределение уровней

Существующие сценарии не правились ни одним: при пороге 3 ни один из них не
меняет ожидаемого уровня. Это и было третьим соображением в пользу 3.

## Сверено оркестратором после реализации

Утверждения шага — утверждения автора о своей работе; проверены прогонами.

**Контрольная пара — сошлась, и по той причине, которая заявлена.** План #84 —
`Уровень: medium`, подсистем 7, и единственный сработавший сигнал `work-breadth`:
других путей-сигналов в этом плане нет, так что уровень поднял именно признак,
а не соседний сигнал. `diff e57dae3^ e57dae3` — `Уровень: low`, подсистем 2,
ни один сигнал не сработал.

**Доля по корпусу — сошлась.** Прогон `history`: `work-breadth` — 16 из 40
(40 %), подсистем на PR p50 = 2, p75 = 5, p90 = 7, максимум = 8. Меньше
половины корпуса, признак критерий проходит.

**Оба харнесса зелены прогоном CI, а не локальным.** `Сценарии риск-скора` —
`Пройдено: 38, провалено: 0, пропущено: 0`, ровно на шесть больше прежних 32.
`Сценарии валидатора плана` (`scripts/plan.test.sh`) — `Пройдено: 30,
провалено: 0`: тот прогон, который шаг не досчитал локально, в CI прошёл, и
пункт «не сделано» закрыт. Локальный повтор остановлен как избыточный.

**Пересчёт уровня по диффу перед ревью** — позиция `diff-rescore` маршрута:

```
bash scripts/risk-score.sh diff main HEAD
Уровень: medium
Факты: файлов 8, строк изменено 1116, подсистем 4 (docs/decisions, docs/tasks, pipeline, scripts)
Сигналы:
  [medium] app-configuration — путь: pipeline/risk.json
  [medium] work-breadth — подсистем 4 (порог 3)
```

Уровень прежний `medium`, на самый строгий маршрут задача не переходит. Маршрут
ревью тот же: обе половины, до двух кругов.

**Расхождение с планом — одно, и оно на решение владельцу.** План задавал
порогом наименьшее число, при котором сигнал срабатывает менее чем на половине
корпуса; по этому правилу выходит **2** (18 из 40, 45 %). Взят **3**. Обоснование
шага записано в `pipeline/risk.json` полем `source` и в записи 0022: 45 % стоит
вплотную к границе отказа, а при пороге 2 `medium` и выше получают 21 из 40 —
больше половины корпуса, то есть меняется смысл уровня `low` для
`pipeline/route.json`, а это предмет записи 0020 и вопрос владельца. Обе
величины стоят числами; смена порога — один символ.

## Починка — круг 1 по замечаниям ревью

**Красной проверки не было ни одной:** все двенадцать чеков PR #121 зелёные или
пропущены по условию. Чинились два замечания половины по корректности, оба —
про прозу, и правка целиком в двух файлах документации. Это тот случай, когда
диффа в исполняемом коде нет законно, а не потому, что чек озеленили
подавлением: сам разбор замечания 1 в ревью заканчивается словами «чинить надо
прозой, а не тестом».

**Причина замечания 1 — запись решения обещала механическую опору, которой у
неё нет.** Абзац «Цена» в 0022 утверждал, что появление нового верхнего
каталога вне списка `subsystems` ловит сценарий «Каждый путь дерева попадает
ровно в одну подсистему списка». Сценарий закрепляет обратное: его вторая
половина подаёт `совсем-новый-каталог/файл.md` и ожидает `подсистем 1
(прочее)` — то есть молчание, а не отказ. Разница не косметическая: запись
решения — то место, откуда читатель узнаёт, на что можно опереться, и ложное
обещание проверки хуже честного её отсутствия.

Проверено, что заявленной проверки и правда нельзя поставить: строгое «ни один
настоящий путь не попадает в „прочее“» падало бы на `.gitignore`, `LICENSE`,
`.dockerignore`. Поэтому абзац теперь называет опору как есть — чтение и
пополнение списка человеком, косвенно просевшая доля срабатывания на следующем
прогоне `history`, — и отдельным абзацем говорит, что сценарий доказывает
другое: при пересекающихся глобах (`docs/rules` против `docs`, `.claude/agents`
против `.claude`) выигрывает первое совпадение и путь не считается дважды.

Соседнее утверждение 0022 про замыкающую запись (строки 40–45) перечитано и
оставлено: оно про отказ кодом 2 при потерянной записи `**`, и эта опора
настоящая — `scripts/risk-score.sh:178`. Формулировка про первое совпадение в
этом журнале (строки о списке `subsystems` выше) ложного обещания не содержала
и не правилась.

**Замечание 2 — строка `note` стояла за пределами таблицы «Проверено»,**
отделённая от неё прозой и нумерованным списком, и отрендерилась бы отдельной
однострочной таблицей без шапки: пункт контракта выпадал из отчёта, который
читают как таблицу. Строка перенесена внутрь таблицы, последней; текст её не
менялся.

Ни `pipeline/risk.json`, ни `scripts/**` эта починка не трогает: поведение
движка замечаниями не оспорено, а починка тестом здесь была бы ровно тем, что
ревью отвергло.

## Починка — круг 2 по замечанию Copilot

**Красной проверки снова не было:** двадцать чеков PR #121 зелёные или
пропущены по условию. Чинился принятый пункт разбора замечаний Copilot — ранний
`**` в списке `subsystems`.

**Причина — проверка конфига закрывала только одну сторону порядка.** Запрос в
`scripts/risk-score.sh` требовал, чтобы шаблон `**` был у последней записи, но
не запрещал его выше. Порядок значим и выигрывает первое совпадение, поэтому
запись с `**`, поставленная первой, забирает все пути: ширина становится
единицей при любой работе, сигнал `work-breadth` не срабатывает никогда, и
скрипт об этом молчит — замыкающая запись формально на месте. Воспроизведено
оркестратором на контрольном плане #84: с жадной записью первой прогон дал
`Уровень: low`, `подсистем 1 (жадная)`, код возврата 0.

Это шестой класс порчи списка в том же выражении, которое PR и заводит:
остальные пять (не массив, пустой при включённом сигнале, потерянная замыкающая
запись, пустой шаблон, дублирующиеся id) уже отказывают кодом 2. Правится в
текущем диффе по разделу «Не расширять рамки»: тот же класс дефекта в том же
месте.

**Сделано** — новая ветвь в том же единственном запросе `jq`, рядом с
проверками записи: при включённом сигнале ширины наличие элемента `**` в
`paths` любой записи, кроме последней, даёт отказ кодом 2 с указанием номера
записи. Предикат — `index("**") != null`, сравнение элементов, а не подстрока:
`docs/**` под него не подпадает, и настоящий конфиг проверку проходит.

Сценарий добавлен шестой мутацией в существующий случай «Нечисловой порог
подсистем и сломанный список подсистем роняют скрипт кодом 2»: конфиг с
`{"id": "жадная", "paths": ["**"]}` первой записью, ожидание — код 2 и фраза
`стоит не последней записью`. Фраза выбрана несовпадающей с сообщением про
потерянную замыкающую запись: замыкание в этой мутации на месте, и пересечение
формулировок дало бы сценарию проходить по чужой причине.

**Второй пункт разбора — незакавыченный ключ ассоциативного массива — отклонён
оркестратором прогоном** и здесь не правился: подстрочник `${hit[$id]}` word
splitting не подвергается. Кавычки безвредны, но правка по ложному обоснованию
кладёт в дерево изменение, которое некому будет объяснить.

Прогоны: `bash scripts/risk-score.test.sh` — 38 из 38, провалено 0, пропущено 0;
контрольный план `bash scripts/risk-score.sh plan scripts/fixtures/plans/84.json`
— `medium`, подсистем 7, тот же перечень, что до правки. То же число повторил CI
на запушенном коммите: работа «Проверки обвязки», шаг «Сценарии риск-скора» —
38 из 38. Все четыре команды `dotnet` (`restore`, `build`, `test`,
`format --verify-no-changes`) прогнаны перед пушем, хотя ни одного файла в
`src/**` починка не трогает: 42 теста, 0 предупреждений, стиль без правок.

**Замеченное про сам харнесс, чтобы следующий заход не тратил на это круг.**
Локально на Windows `risk-score.test.sh` идёт десятки минут — время уходит на
git во временных репозиториях, — тогда как в CI на Linux тот же прогон занимает
восемь секунд. Один прогон из трёх дал провал сценария «Три первых зоны правил
безопасности опознаются путём на настоящих файлах дерева», которого починка не
касается, и пришёлся он ровно на минуты, когда рядом шли `dotnet restore` и
`git commit`. Два других прогона и CI его проходят. Признаки случайного по
`docs/rules/fix.md` налицо: формулировка не связана с правкой, воспроизведения
нет. Заводить задачу не на что, пока не повторится без нагрузки рядом.

## Починка — круг 3 по замечанию оркестратора

**Красной проверки опять не было:** 13 чеков PR #121 зелёные, ноль красных.
Чинилось замечание сверки оркестратора — запись 0022 описывала защиту списка
подсистем односторонне.

**Причина — правило заведено двумя коммитами, а запись решения писалась под
первый.** Абзац «Что считается подсистемой» (0022:40–45) появился вместе с
проверкой потерянной замыкающей записи и называл только её. Вторая половина —
отказ на шаблоне `**` в записи, которая не последняя, — пришла позже, починкой
круга 2 (`5c4212e`), и уехала в `pipeline/risk.json` (`subsystems_about`) и в
сообщение `scripts/risk-score.sh:197`, а в запись решения нет. Круг 1 ревью
абзац перечитывал и оставил — тогда он был полным; неполным его сделала
следующая правка, и это ровно тот случай, когда описание защиты расходится с
защитой не по ошибке автора, а по времени.

Класс тот же, за который круг 1 выписал major на «Цену»: запись решения
описывает механическую защиту не такой, какая она есть. Разница в знаке — там
обещала лишнее, здесь недоговаривала.

**Проверено первичным источником, а не таблицей:** обе ветви запроса гейтятся
одинаково — `$breadth > 0`, то есть отказывают только при включённом сигнале
ширины (`scripts/risk-score.sh:176–178` и `:194–197`). Это и было единственным
фактом, от которого зависела формулировка: написать «роняет кодом 2» без
оговорки о включённости значило бы положить в защищённый файл новый вариант
того же дефекта.

**Сделано** — в тот же абзац дописана вторая половина правила: обе стороны
стоят рядом, названы одинаковый код отказа, одинаковый гейт по включённости и
одинаковое следствие (ширина становится единицей, сигнал молча не срабатывает
никогда). Больше в записи ничего не менялось; `pipeline/risk.json` и
`scripts/**` эта починка не трогает — там обе стороны уже описаны, а поведение
движка замечанием не оспорено.

Счёт «пять классов порчи списка» из круга 2 в дереве не задержался: в журнале
он с самого начала записан как шестой (строка 463), в `0022`, `risk.json` и
шапках скриптов числа классов нет вовсе — сверено грепом по слову «класс».

Повторное ревью не запрашивается: машинные круги исчерпаны (корректность 2 из 2
по потолку уровня `medium`, запись 0016), а замечание пришло не от круга ревью,
а от сверки оркестратора.

**Прогоны, и откуда взято каждое число.** Авторитет здесь — CI на запушенном
коммите `5d094bd`: работа «Проверки обвязки», шаг «Сценарии риск-скора» —
«Пройдено: 38, провалено: 0, пропущено: 0», восемь секунд. Локальный прогон
`bash scripts/risk-score.test.sh` на той же правке дал 37 из 38: провалился
сценарий «Дифф только в тестах и изолированной логике даёт low без единого
сигнала» — код возврата 1 вместо 0 при правильном выводе (`Уровень: low`,
сигналов ноль). Команды `dotnet` не запускались: правка целиком в двух файлах
прозы, ни одного файла C# в диффе нет — тот же довод, что в теле PR.

Локальный провал — второй случай подряд и снова под нагрузкой: рядом шёл
`gh pr checks --watch`, а сценарий, как и в круге 2, каждый раз другой и правкой
не затронут физически (дифф — два файла Markdown). Условие круга 2 «заводить
задачу не на что, пока не повторится без нагрузки рядом» так и не выполнено:
повтор снова под нагрузкой. Практический вывод для следующего захода — числа
харнесса на этой машине брать из CI, а локальный прогон держать наблюдением, а
не опорой отчёта.

## Осталось

Черновик с PR снимает человек; порог ширины (3 против вычисляемого правилом 2)
остаётся его же решением.
