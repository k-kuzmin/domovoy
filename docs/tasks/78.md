# Задача #78 — рамка определения step-triage узка для его собственных правил

**Стадия:** реализация
**Ветка:** `feat/78-step-definition-frames`
**PR:** #80 (черновой)
**Следующий шаг:** решить, как быть с `'pwd'` в списках пяти читающих
определений: правка отбита классификатором прав локальной сессии (см.
«Осталось»), остальные две правки лежат в ветке. После этого — ревью, правка
замечаний и только затем два контрольных прогона `step-triage` на #71 и #75,
**в новой сессии CLI**: `.claude/agents/` читается при старте, и замер в той
же сессии, где правились файлы, может прийтись на старую рамку

- [x] разбор — вердикт триажа получен, семь вопросов заданы, ответы есть
- [x] план — одобрен владельцем, `pwd` уточнён до пяти читающих шагов
- [ ] реализация — две правки из трёх сделаны, `pwd` заблокирован
- [ ] ревью
- [ ] починка

## Границы

**Входит:** frontmatter и тело определений `.claude/agents/step-*.md` —
потолок `maxTurns`, список разрешённых команд Bash, правила чтения в теле.

**Не входит:**

- `scripts/step-bash-allow.sh` — по тексту самой задачи: он отработал ровно
  как описан. Пока владелец явно не расширит объём, вариант «научить разбор
  случаю `2>&1`» недоступен, и остаётся только объявить идиому недоступной
  в теле определения;
- `scripts/rules-sync.sh` — расширение его до проверки frontmatter задачей не
  ставится (см. «Осталось»);
- проверка копий внутри тела определения — #75.

## Вопросы и ответы

| Вопрос | Ответ | Кто ответил |
|---|---|---|
| Метка `agent/allow-protected` на задаче | Поставлена | владелец |
| Потолок `maxTurns` у триажа | 15 → 25 | владелец |
| Ставить ли потолок пяти определениям без него | Нет, остаются без потолка | владелец |
| `pwd` и `cd` | `pwd` выдать, `cd` запретить объяснением в теле | владелец |
| Правило «файлы читаются `Read`, а не `head`» | Во все семь определений | владелец |
| `2>&1` | Объявить недоступной идиомой в теле | владелец |
| На чём прогонять два контрольных прогона | #71 и #75, по одному на каждой | владелец |
| `pwd` — пяти читающим шагам или всем семи | Пяти. У `implement` и `fix` есть `git status`, отвечающий на тот же вопрос богаче | владелец |
| Когда делать контрольные прогоны | После ревью, отдельным заходом в новой сессии | владелец |

## Решения

Все семь записаны комментарием в issue 78 **до** правок — это буква критерия
готовности, а не формальность: решение, принятое агентом молча, через месяц
неотличимо от недосмотра. Здесь только то, что не сводится к таблице выше.

**Потолок поднят, но пяти шагам не поставлен.** Разница в цене отказа. Триаж
ничего не пишет и идёт на младшей модели: цена зацикливания — деньги, цена
обрыва — потерянный вердикт, оба дёшевы. У реализации и починки цена обрыва
на середине — незакоммиченные правки, которых не существует ни для кого,
кроме текущей сессии. Потолок там был бы отказом по случайному поводу, тем
более что числа взять неоткуда: наблюдений по этим пяти шагам нет.

**Граница команд не трогается ни в одном из решений.** `pwd` добавляется в
список разрешённого — это аргумент хука в определении, а не сам хук. `cd` и
`2>&1` лечатся строкой в теле, а не исключением в разборе: каждое исключение
в границе придётся покрыть тестом, защитить от обхода и объяснять каждому,
кто читает скрипт. Причина отказа и так доходит до субагента текстом и шаг не
роняет — это наблюдение #77.

**Правило про `Read` — дополнение к `docs/rules/reading.md`, а не копия его.**
`reading.md` говорит «сначала отбор по признакам, потом чтение целиком» и про
выбор инструмента чтения не говорит вовсе. Именно на этом выборе разошлись
два замера в #77. Тела определений в проверку копий сегодня не входят (#75),
поэтому от пересказа `reading.md` своими словами удерживает только автор.

**Классификатор прав локальной сессии отбил расширение списка команд.**
Правка `'pwd'` в аргументы хука `PreToolUse` не прошла: «Permission for this
action was denied by the Claude Code auto mode classifier». Правка `maxTurns`
в том же файле и правки тела прошли — значит блокируется не защищённый путь
как таковой, а именно расширение списка разрешённых команд Bash. Это
согласуется с правилом, которое стоит над задачей: согласие человека в issue
и метка на задаче не авторизуют агента править конфигурацию собственных прав.
Отвергнуто «сделать то же другим инструментом»: обход отказа классификатора —
ровно то, против чего он поставлен, и цена такого обхода не в этой задаче, а
в том, что граница перестаёт что-либо значить. Правка вынесена в тело PR как
расхождение с планом.

## Изменённые файлы

| Файл | Зачем |
|---|---|
| `docs/tasks/78.md` | журнал задачи |
| `.claude/agents/step-triage.md` | `maxTurns: 15 → 25`; два абзаца в тело |
| `.claude/agents/step-plan.md` | два абзаца в тело |
| `.claude/agents/step-review-correctness.md` | то же |
| `.claude/agents/step-review-security.md` | то же |
| `.claude/agents/step-fix-flaky.md` | то же; `maxTurns: 10` не тронут |
| `.claude/agents/step-implement.md` | то же |
| `.claude/agents/step-fix.md` | то же |

Два абзаца — это три решения владельца: чем читается файл репозитория
(`Read`, а `head`/`tail` — для отбора в выводе команд), почему не нужен `cd`
и почему не проходит `2>&1`. Правило отбора осталось в
`docs/rules/reading.md` и своими словами не пересказано: в определении стоит
только то, чего в `reading.md` нет вовсе.

## Проверено

- шаг `step-triage` на задаче 78 — вердикт получен, «не готова», 12 вызовов
  инструментов. За контрольный по критерию 4 не считается: сделан **до**
  правок и на самой задаче #78.
- `bash scripts/rules-sync.sh` — сходится: правил 8, промптов 5, определений 7;
  `rules-sync.test.sh` — 25 из 25.
- `bash scripts/wiring.sh` — сходится: скриптов 10, workflow 9;
  `wiring.test.sh` — 12 из 12.
- `bash scripts/step-bash-allow.test.sh` — 48 из 48;
  `bash scripts/guard.test.sh` — 22 из 22.
- `dotnet build --no-restore -c Release` — ошибок 0;
  `dotnet test --no-build -c Release` — 42 из 42;
  `dotnet format --verify-no-changes --no-restore` — без замечаний.
- `grep` по самим определениям — потому что `rules-sync.sh` frontmatter не
  смотрит и зелёный её цвет о правке рамок не говорит ничего: `maxTurns`
  ровно две строки (25 у триажа, 10 у `fix-flaky`), новый абзац тела — по
  одному попаданию в каждом из семи файлов, `'pwd'` — ноль попаданий.

## Что установлено при разборе

Не вердикт шага — наблюдения оркестратора по самим определениям.

- **`maxTurns` есть только у `step-triage` (15) и `step-fix-flaky` (10).**
  У пяти остальных поля нет вовсе. Поэтому вопрос «тот же потолок остальным
  шести» на деле звучит иначе: ставить ли потолок там, где его нет.
- **`scripts/rules-sync.sh` frontmatter не проверяет** — ни `model`, ни
  `maxTurns`, ни список команд хука. Критерий «rules-sync зелёный» проверяет
  связность ссылок и ничего не говорит о корректности правок рамок.
- **`maxTurns` считает ходы модели, а не вызовы инструментов** (журнал #77).
  Замер 2 уложился при большем числе вызовов, чем замер 1: пачка вызовов в
  одном ходе стоит одного хода.

## Осталось

- **`'pwd'` в списках пяти читающих определений** — `step-triage`,
  `step-plan`, `step-review-correctness`, `step-review-security`,
  `step-fix-flaky`. Одно слово в конец существующей строки списка в хуке
  `PreToolUse`; отдельной формы записи не нужно — совпадение в
  `scripts/step-bash-allow.sh` идёт по началу строки. Правку делает человек:
  агенту она отбита классификатором прав (см. «Решения»).
- **Два контрольных прогона `step-triage` на #71 и #75** (критерий готовности
  4) — после ревью и правки замечаний, в новой сессии CLI, силами
  оркестратора. Свидетельство — стенограммы: вердикт, `agentType`, число
  ходов, число отказов границы. Ожидание по отказам — ноль, и ноль разного
  происхождения: `pwd` проходит (если строка списка к тому моменту
  добавлена), `cd` и `2>&1` не вызываются вовсе.
- **Метку `agent/allow-protected` на PR #80** ставит оркестратор; до этого
  `scripts/guard.sh` на нём красный по правке `.claude/agents/**`.
- отдельно от этой задачи: `rules-sync.sh` не видит frontmatter, поэтому
  рассогласование `maxTurns` или списка команд с намерением ничем
  не ловится. Если это стоит проверки — заводить отдельную issue, а не
  расширять эту.
