# Задача #65 — правила агента вынести из спящих процессов

**Стадия:** ревью
**Ветка:** `feat/65-rules-out-of-workflows`
**PR:** #66
**Следующий шаг:** снять с PR #66 черновик — это запускает ревью. Сделаны все
четыре пункта задачи и оба пункта, которые владелец добавил после отчёта:
прямое плечо цикла и механическая сверка правил с промптами. Открытых вопросов
нет.

- [x] разбор — границы ясны, вопросы заданы
- [x] план — опубликован комментарием, владелец дал добро начинать
- [x] реализация
- [ ] ревью
- [ ] починка

## Границы

Входит:

- перенос правил из промптов `.github/workflows/agent-*.yml` в `docs/rules/`
  с заменой текста на ссылки и указателем из `.claude/CLAUDE.md`;
- пункт ревью корректности: сверка сделанного с утверждённым планом;
- момент создания чернового PR — по готовности реализации, а не по первому
  зелёному прогону; половина «ветка пушится рано и часто» не двигается;
- возвратное плечо цикла: перезапуск ревью после починки, потолок 2 круга,
  метки `review/1`, `review/2`, на третьем круге `agent/needs-human`.

**Рамки расширены владельцем после отчёта по первым четырём пунктам:** прямое
плечо цикла «вердикт → починка» и механическая сверка правил с промптами
перенесены из «не входит» в работу. Отдельными задачами они не заводились —
обе правят те же файлы, что PR #66, и отдельные PR конфликтовали бы с ним.

Не входит:
- разбивка `CLAUDE.md` на тонкое ядро и темы — отложено владельцем в #63;
- `labeled` у `ci-fast.yml`, дубли gitleaks, кэш NuGet для CodeQL — найдено
  при разборе пайплайна в #63, к правилам отношения не имеет;
- потолки кругов и попыток не пересматривались: два и три соответственно.

## Вопросы и ответы

| Вопрос | Ответ | Кто ответил |
|---|---|---|
| Счётчик кругов ревью — общий с `fix/1..3` или отдельный | Отдельный: `review/1`, `review/2`. Поводы разные — падение CI и замечание ревьюера не одно и то же | владелец (комментарий в #65) |
| Момент создания чернового PR | Порядок прежний (PR → ревью → исправление), но черновой PR открывается по готовности реализации. «Ветка пушится» остаётся как есть — это защита от потери, а не видимость | владелец (комментарий в #65) |
| Прямое плечо цикла «вердикт `changes_requested` → `agent-fix`» — входит в #65 | Входит. Как и механическая сверка правил: «доделывай плечо и сверку» | владелец |
| Чем запускать починку, если формальный отзыв невозможен | Комментарий `/fix`. Метка отвергнута: метки здесь делятся на разрешения человека и отчёты агента, команде среди них места нет. `repository_dispatch` отвергнут: невидим в переписке PR | — |
| Куда встроить сверку правил | Отдельная работа в `CI (fast)`, а не шаг гейта целостности: гейт смотрит дифф и живёт только на PR, а сверка — инвариант репозитория и нужна на push в `main` тоже (документацию туда коммитят напрямую) | — |
| Пять файлов правил или шесть | Спросить некому. Допущение: шесть — ревью разнесено на корректность и безопасность, потому что два job разделены намеренно. Один файл на два job вернул бы ревьюера, который заканчивает список после третьего пункта | — |

## Решения

**Правила — слой поверх `CLAUDE.md`, а не самостоятельный документ.** Запреты
(подавление предупреждений, отключение тестов, правка пайплайна) остаются
каноническими в разделе «Запрещённые способы „починить“ сборку»,
`docs/rules/implement.md` и `fix.md` на него ссылаются. Отвергнуто: сделать
файлы правил самодостаточными — вместо двух копий стало бы три, ровно то,
против чего задача.

**Возвратное плечо замыкается комментарием `/review`, а не триггером
`synchronize`.** Отвергнуто добавление `synchronize` в `on:` — формулировка
самой задачи, но оно срабатывает на каждый пуш в PR, включая пуши по ходу
реализации, и без дополнительного условия оплачивало бы два ревью на каждый
коммит. `agent-fix`, запушив починку по замечаниям, сам пишет комментарий
`/review` через AGENT_TOKEN: событие от PAT запускает workflow, вход
существующий, и срабатывает он только там, где починка была вызвана ревью.

**Счётчик кругов — отдельный job до вызова модели.** Дублировать шаг счёта
внутри `correctness` и `security` нельзя: они гонялись бы за право поставить
`review/N`, и оба сожгли бы вызов модели на стоп-круге. Job `round` считает
круг по меткам и отдаёт `stop`, два ревью встают на него через `needs:`. Та же
дисциплина, что у `fix/N` в `agent-fix.yml`: метка ставится до работы, потому
что считает круги, а не успехи.

**Отмена на середине теперь съедает круг — `cancel-in-progress: false`.**
Замечено при сверке уже после реализации: счётчик сделал `agent-review`
процессом с состоянием, а значение `cancel-in-progress: true` осталось от
времён, когда отменённое ревью не стоило ничего. Метку `review/N` ставит job
`round` до ревью — значит отмена между ними тратит круг и не даёт замечаний.
Ровно то же соображение и та же правка, что в `agent-fix.yml`, где счётчик
появился раньше. Подробности — в записи 0016.

## Найдено при разборе

**Прямое плечо цикла ревью, похоже, недостижимо.** `agent-review.yml` печатает
вердикт через `gh pr comment` и никогда не делает
`gh pr review --request-changes`. А `agent-fix.yml` ждёт события
`pull_request_review` с `state == changes_requested` и
`author_association == 'OWNER'`. Ветку и PR создаёт `agent-implement` через
AGENT_TOKEN, то есть автор PR — владелец, а GitHub запрещает запрашивать
изменения в собственном PR. Если это так, замыкание возвратного плеча даёт
цикл, у которого не запускается прямое. Ограничение проверяется вызовом API на
живом PR этой задачи; рамки — за владельцем.

**Соответствие шага и его правил выводится из имён файлов.** В
`scripts/rules-sync.sh` нет таблицы «какой промпт к каким правилам»: шаг берётся
из имени `agent-<шаг>.yml`, правила — файлы, чьё имя начинается с того же слова.
Отвергнут захардкоженный список: он был бы копией, разъезжающейся наравне с
тем, что проверяет, — то же соображение, по которому тест инструментов
перечисляет регистрации DI, а не сверяется со списком.

**Проверка ветки `feat/` для входа `/fix` переехала внутрь шага.** У события
`issue_comment` ветки в payload нет вовсе, поэтому в `if` на уровне job её не
выразить. Для двух других входов проверка осталась в гейте; для третьего она
стоит там, где ветка уже добыта, — второй рубеж, а не дубль. Основной барьер
для `issue_comment` тот же, что у `/review`: автор комментария — владелец.

## Изменённые файлы

| Файл | Зачем |
|---|---|
| `docs/rules/README.md` | Указатель по шагам; общие уровни замечаний blocker/major/nit — чтобы два файла ревью не держали по копии |
| `docs/rules/triage.md` | Пять критериев готовности задачи |
| `docs/rules/plan.md` | Шесть разделов плана, что делать, когда план составить нельзя |
| `docs/rules/implement.md` | Порядок работы; запреты — ссылкой на `CLAUDE.md`; новый момент создания чернового PR |
| `docs/rules/review-correctness.md` | Пункты корректности; добавлен пункт 2 — сверка с утверждённым планом |
| `docs/rules/review-security.md` | Пункты безопасности и список «не применимо сейчас» |
| `docs/rules/fix.md` | Причина вместо проявления, минимальность, признак имитации, признаки флаки; чем заканчивается починка по замечаниям |
| `.claude/CLAUDE.md` | Раздел-указатель на `docs/rules/`; `docs/rules/**` в защищённых путях; строка в таблице синхронизации; раздел «Журнал задачи» разделяет пуш ветки и открытие PR |
| `.github/workflows/agent-triage.yml` | Промпт — ссылка вместо текста |
| `.github/workflows/agent-plan.yml` | То же |
| `.github/workflows/agent-implement.yml` | То же; из промпта ушли 8 шагов и запреты |
| `.github/workflows/agent-review.yml` | Ссылки; job `round` со счётчиком и потолком; `Bash(gh issue view:*)` у корректности — иначе пункт про план невыполним |
| `.github/workflows/agent-fix.yml` | Ссылки; шаг «Ревью заново»; признаки флаки из промпта оценки тоже ушли в `fix.md` |
| `scripts/guard.sh` | `docs/rules/.+` в защищённых путях |
| `scripts/guard.test.sh` | Сценарий 1г: правила шагов защищены |
| `scripts/bootstrap.sh` | Метки `review/1`, `review/2` |
| `.github/CODEOWNERS` | `/docs/rules/` — третье из трёх мест, описывающих одно множество |
| `docs/decisions/0015-…md` | Момент создания чернового PR |
| `docs/decisions/0016-…md` | Отдельный счётчик кругов ревью, потолок 2, `/review` вместо `synchronize` |
| `docs/decisions/README.md` | Строки указателя 0015, 0016 |
| `docs/pipeline.md` | Схема цикла, таблицы меток и «Прерывание и продолжение», потолок кругов, проза про черновой PR, рычаги `/review` и `/fix`, новая required-проверка |
| `scripts/rules-sync.sh` | Сверка правил с промптами и указателем: пять проверок |
| `scripts/rules-sync.test.sh` | Восемь сценариев: сверка обязана падать на каждом виде расхождения |
| `.github/workflows/ci-fast.yml` | Работа «Правила шагов и промпты» — сверка и её харнесс |
| `docs/decisions/0017-…md` | Починку запускает комментарий `/fix`, а не отзыв на PR |

## Проверено

- **Прямое плечо цикла действительно недостижимо.** Проверено вызовом API на
  живом PR #66: `gh api repos/…/pulls/66/reviews -f event=REQUEST_CHANGES`
  отвечает `422 Unprocessable Entity`, «Review Can not request changes on your
  own pull request». Догадка из разбора стала фактом: пока автор PR — владелец
  (а ветку пушит `agent-implement` его же токеном), путь
  `pull_request_review` в `agent-fix` запускается только человеком вручную.
- YAML всех девяти процессов разбирается — `yaml.safe_load`, 9 из 9.
- `scripts/guard.test.sh` — 20 сценариев из 20, включая новый на `docs/rules/`.
- Полнота переноса по нумерованным пунктам прежних промптов: 5 из 5 у триажа,
  6 из 6 у плана, 8 из 8 у реализации, 4 из 4 у починки, 13 из 13 у обоих
  ревью. Расхождения только в форме — императив промпта («Найди причину») стал
  нормой правила («Найти причину»), поэтому дословная сверка строк давала
  ложные потери и пункты сличены попарно.
- Логика счёта кругов на наборах меток: пусто → круг 1; `review/1` → 2;
  `review/1`+`review/2` → стоп. `fix/1..3` на счёт не влияют; `review/10` не
  даёт ложного срабатывания (`grep -qx`).
- Ни один пункт прежних промптов ревью не потерялся: из 16 заголовков в старой
  версии `agent-review.yml` в `docs/rules/` нашлись все 15 содержательных
  (шестнадцатый — `$verdict` из shell-эха).
- Поиск по `agent-*.yml` после переноса: содержательных критериев в промптах не
  осталось, совпадения — только имена полей схемы (`blocker_count`), ссылки на
  разделы `CLAUDE.md` и `if-no-files-found: ignore`.
- Метки `review/1`, `review/2` заведены `scripts/bootstrap.sh` (16 меток,
  скрипт идемпотентен), проверено `gh label list`.
- `gitleaks git --staged` — чисто на каждом из пяти коммитов.
- Гейт целостности на PR #66 — зелёный с первого прогона: метка
  `agent/allow-protected` поставлена **при создании** PR (`gh pr create
  --label`), а не после. Ровно та грабля, на которую наступили в #63.
- `scripts/rules-sync.sh` на репозитории — сходится (7 файлов правил,
  5 промптов). `scripts/rules-sync.test.sh` — 8 сценариев из 8: сверка падает
  на переименованном файле правил, на правилах без ссылок из промптов, на
  промпте, потерявшем ссылку на свои правила, на файле, отсутствующем в
  указателе, на битой относительной ссылке и на шаге без правил.
- Условия всех трёх входов `agent-fix` разобраны по payload: `workflow_run`
  даёт ветку без номера PR, `pull_request_review` — оба, `issue_comment` —
  номер без ветки. Последний случай и был причиной перенести разрешение ветки
  до `checkout`.
- Локально `dotnet build` / `dotnet test` не запускались: прикладной код не
  тронут, дифф целиком в правилах, промптах и обвязке. Сборку и тесты прогнал
  CI.

## Осталось

**Правки не проверены на живом прогоне** — цикл выключен
(`vars.AGENT_LOOP_ENABLED` не задана), настоящего ревью и настоящей починки, на
которых сработали бы job `round` и шаг «Ревью заново», взять негде. То же
ограничение, что в #63.

**Не проверено, хватает ли `pull-requests: write` для метки на PR.** Job
`round` ставит `review/N` тем же способом, которым `agent-fix` ставит `fix/N`,
то есть повторяет существующий и тоже ни разу не проверенный путь. Если
понадобится `issues: write` — узнается на первом живом прогоне.

**Сверка не читает содержимое правил, только связи.** `rules-sync.sh` поймает
переименованный файл, потерянную ссылку и отсутствие в указателе, но не
поймает промпт, который ссылается на правила и рядом же пересказывает их
своими словами. Это то же ограничение, что у любой проверки ссылок, и лечится
оно не скриптом, а ревью.

**`scripts/guard.test.sh` по-прежнему не запускается в CI** — замечено попутно,
когда встраивал `rules-sync.test.sh`. Харнесс гейта целостности есть, но его
никто не прогоняет автоматически. В рамки этой задачи не входит: отдельная
issue.
