# Задача #113 — маршрут задачи зависит от её уровня риска

**Стадия:** починка по замечаниям круга 1 сделана; круг 1 ревью закрыт
**Ветка:** `feat/113-route-by-risk-level`
**PR:** #114 (черновик), метка `agent/allow-protected` стоит, все чеки до
починки зелёные
**Следующий шаг:** круг 2 ревью PR, обе половины — уровень `medium`, второй
круг по усмотрению, но первый чистым не был (`changes_requested` от
безопасности), а состав ревью не сокращается ни на каком уровне. Прогоны после
починки оркестратор сделал: `route.sh check` — код 0, харнесс — 15 из 15.
Дальше снятие черновика человеком

- [x] разбор — границы ясны, вопрос один: метка. Поставлена владельцем
- [x] план — круг 2 согласован владельцем; апрув круга 1 сознательно не
      наследован, подтверждение получено отдельно
- [x] реализация — весь состав плана. Шаг сделал всё, кроме строки
      `owner: orchestrator`, и не мог запустить два новых скрипта: их команд
      нет в его списке разрешённых. Оркестратор дописал `.claude/CLAUDE.md` и
      прогнал обе — движок на трёх уровнях, `check` и харнесс, 15 из 15
- [x] ревью — круг 1: корректность `approve`, безопасность `changes_requested`
      (три major, одно вынесено в отдельную issue владельцем)
- [x] починка — круг 1 по замечаниям: два major политики, оговорка в
      `docs/pipeline.md`, адресат прогона в трёх правилах, сценарий обвязки

## Границы

Границы закреплены планом круга 2 — поля `boundaries` и `out_of_scope`. Против
круга 1 в границы добавлен `scripts/risk-score.test.sh` (замечание 3). Сюда
они не переписываются: копия разъедется с планом молча, а план живёт
комментарием в задаче.

Что разбор оставлял под вопросом и как это разрешилось:

- **`.github/workflows/agent-*.yml`** — не входит. Цикл выключен, у плановой
  половины нет живого прогона, а токен без разрешения Workflows не может
  запушить правку `.github/workflows/**`. Цикл остаётся на самом строгом
  маршруте — это отсутствие потребителя, а не расходящаяся копия;
- **`pipeline/`** — входит: политика вторым файлом `pipeline/route.json`, и
  этой же задачей `pipeline/**` закрывается гейтом;
- **`scripts/`** — входит: движок `route.sh` с подкомандой `check` — той
  механической сверкой маршрута, которой у `rules-sync.sh` нет;
- **правила шагов** — входят, но не новым файлом: три существующих правила
  утверждают маршрут безусловно, и утверждение заменяется отсылкой к
  источнику;
- **#71** — раньше не требуется: маршрут выражается тем, какие шаги
  запускает оркестратор, а не порядком «CI → ревью».

## Вопросы и ответы

| Вопрос | Ответ | Кто ответил |
|---|---|---|
| Метка `agent/allow-protected` на задаче — правка задевает `.claude/CLAUDE.md`, `docs/decisions/**` и, вероятно, `.github/**` и `scripts/**` | поставлена. На PR понадобится своя: гейт целостности смотрит на метки PR, а не задачи | владелец |
| Верен ли вердикт триажа про метку | сверено: `gh issue view 113 --json labels` → `area/infra`, `type/chore`, `priority/normal`; метки нет. Пункт 4 `docs/rules/triage.md` делает это первым вопросом человеку | оркестратор, прогон в дереве |
| Состояние задач, на которые задача ссылается | сверено: #71 открыта, #95 открыта, #103 закрыта | оркестратор, прогон в дереве |
| Существуют ли адреса `файл:строка`, на которых держится подход плана | сверено прогоном все девять; один адрес круг 1 поправил: цитата про плановый круг — `plan.md:122`, не `:124`. Остальные: `review-correctness.md:15`, `fix.md:53`, `protected-paths.sh:43`, `CODEOWNERS:33`, `rules-sync.sh:667` (`COPY_WINDOW=10`), `plan-review-round.sh:44` (`CEILING=2`), `CLAUDE.md:440`, `risk-score.sh:63` | оркестратор, прогон до публикации плана |
| Действительно ли `pipeline/**` не защищён | `grep -n pipeline` по `scripts/protected-paths.sh` и `.github/CODEOWNERS` — ноль попаданий в обоих | оркестратор, прогон в дереве |

## Решения

Обоснования и отвергнутые варианты живут в плане (поля `approach.summary` и
`approach.rejected`) и переживут задачу записью `docs/decisions/0020` — здесь
не дублируются. Что стоит записать сверх плана:

**Формат политики решён не этой задачей.** `docs/decisions/0019` уже отклонил
`pipeline/*.yaml` в пользу JSON, и причина — отсутствие парсера YAML в
обвязке — от маршрута не зависит. Открыт был только вопрос «тот же файл или
второй»; план выбрал второй, потому что «насколько рискованно» и «как
проверяем» — разные вопросы с разными читателями. Запись 0019 сама
предупреждала, что к фазе 7 `pipeline/**` придётся закрыть гейтом, и обещала
это отдельной задачей: обещание выполняет эта.

**Проверка политики на самой этой задаче.** `bash scripts/risk-score.sh plan`
по опубликованному плану даёт `medium` — сигнал `app-configuration` на
`pipeline/route.json` и `pipeline/risk.json`. То есть задача, вводящая
сокращение маршрута, сама сокращения не получает и идёт полным обрядом. Это и
есть различающая проверка: выдай политика этому PR короткий маршрут, неверна
была бы политика, а не прогон.

**Круг 1 ревью плана — `changes_requested`, все пять замечаний сверены
прогоном и ни одно не отклонено.** Существенное — блокер: маршрут `high`
сегодня не строже `medium`. Позиции 1–3 у них совпадают дословно, позиция 4
(«пересчёт уровня по диффу») на `high` — послабление, а позиция 5 («мердж
только человеком») инертна: `docs/pipeline.md:615` — «**Allow auto-merge** —
включить», незакрытый пункт списка ручных настроек, `:885`. То есть на `high`
не появляется ни одного действия, которого нет на `medium`, а одно исчезает,
и правило ТЗ «на `high` человек обязателен везде» не закодировано, а обещано
на день включения настройки — то самое «обещание настройки, которой нет»,
которым `pipeline/risk.json:3` объясняет отказ выносить уровни в конфиг.

Второе следствие того же дефекта: сверка `check` и сценарий «три разных
маршрута» сравнивают **тексты** позиций, поэтому пара `medium`/`high`,
различающаяся формулировкой инертной строки, проходит их зелёными — тест не
отличает работающее от сломанного.

Три major — механические, уходят в круг 2 как есть: строка реестра
`docs/decisions/README.md` для 0020 отсутствует в разделе «Файлы» (реестр
держит 0001–0019 без пропусков, машинной проверки у него нет);
`scripts/risk-score.test.sh:560-561` содержит то же становящееся ложным
утверждение про незащищённость `pipeline/**`, а путь не входит ни в границы, ни
в «Файлы» — шаг реализации упёрся бы в отказ на середине; существование
потребителя из `consumers` не проверяется, хотя для `decision` проверка
требуется.

Nit касается и моего блока «Сверено прогоном» под планом: цитата про плановый
круг стоит на `docs/rules/plan.md:122`, а не `:124`. Строка 124 существует и
говорит о том же круге, но первая строка цитаты — 122.

**Блокер решён владельцем: `high` ужесточается по-настоящему**, а не получает
запись о совпадении с `medium`. На `high` оба круга ревью PR идут всегда, даже
когда первый чист (на `medium` второй — по усмотрению), и послабление позиции 4
снято: пересчёт по диффу обязателен и на `high`. Так у `high` появляется
действие, которого нет на `medium`, и оно работает без единой правки настроек
GitHub. Позиция «мердж только человеком» остаётся, но перестаёт быть
единственным различием уровней, а её инертность названа полем `note`.

Отвергнут третий вариант — включить авто-мердж в настройках репозитория, чтобы
позиция 5 заработала. Тогда первый же `low`/`medium` PR начал бы вливаться сам,
включая PR этой задачи, и настройка опередила бы маршрут, который её описывает.
Пункт «Allow auto-merge» остаётся незакрытым в списке ручных настроек.

**Круг 2 закрыл все пять замечаний.** Механическую половину блокера — без
нового поля политики: раздел «ЧЕГО НЕ ЛОВИТ» в шапке движка, отдельный риск и
требование к сценарию, чтобы различие пары `medium`/`high` приходилось на
позицию `pr-review-rounds`, а не на инертную строку мерджа. Вариант с полем
«работает сегодня» отвергнут в плане с причиной: это та же настройка, которой
нет, — довод `pipeline/risk.json:3`, которым круг 1 и разбил позицию мерджа.

**Метка `plan/approved` разошлась с состоянием, и это разрешает владелец.**
Порядок событий по таймлайну задачи: план круга 1 опубликован 12:46:37Z, метка
`plan/approved` поставлена 13:01:14Z, вердикт ревью с блокером — 13:03Z. То
есть метка одобряет круг 1 — план, который ревью вернуло. Круг 2 изменил
существо: ужесточился `high`, добавились два файла и один инвариант сверки.
Наследовать апрув молча нельзя ровно потому, что он стоит в списке «не
сокращается никогда». Владелец подтвердил апрув на круг 2 отдельно — «апрув в
силе», — и только после этого началась реализация. Метку оркестратор не снимает: снятие чужого апрува —
тоже действие владельца.

В таймлайне все операции с метками числятся за владельцем, потому что `gh`
работает под его токеном; различить по актору свои правки и чужие нельзя.
Основание считать метку чужой — не актор, а перечень собственных команд:
`--add-label plan/approved` оркестратор не запускал ни разу.

### Решения реализации

**Ключ `--repo` у движка — сверх плана.** План называл у `route.sh` только
`--config`, но два его же сценария («потребитель без ссылки», «дословная копия
значения») требуют временного дерева: портить настоящих потребителей харнесс не
вправе. Ключ повторяет `--repo` у `scripts/risk-score.sh` и меняет только то,
относительно чего разрешаются пути потребителей и записи решения. Строка,
наличие которой сверяется у потребителей, при этом константа —
`pipeline/route.json`, расположение политики по умолчанию: иначе временная
копия политики требовала бы у потребителей ссылки на временный путь.

**Один сценарий сверх таблицы плана — «Движок не содержит знания о маршруте».**
Пункт приёмки `policy-one-file` план проверял вручную: `grep` по списку слов из
комментария к задаче. Сценарий делает то же механически и без списка слов:
перечень берётся из политики — идентификаторы позиций, все значения, пути
потребителей, запись решения, `applied_by` — и падает, если что-то из этого
переехало в код движка. Список слов в харнессе устаревал бы молча, перечень из
политики — нет.

**Позиция мерджа не помечена сокращённой ни на одном уровне.** Доказательство
пункта `low-named` в плане называет три сокращённые позиции поимённо
(`plan-review-machine`, `plan-review-rounds`, `pr-review-rounds`), и мерджа
среди них нет. Формально «авто-мердж допустим» — послабление против «мердж
делает человек», но послаблять сегодня нечего: авто-мерджа в репозитории нет.
Отметка `reduced` при инертной позиции обещала бы сокращение, которого не
происходит, — то же «обещание настройки, которой нет». Причина стоит в поле
`note` самой позиции, а не только здесь.

**Оба новых прогона шагу реализации не выданы.** `bash scripts/route.sh` и
`bash scripts/route.test.sh` не входят в список разрешённых команд
`step-implement`, поэтому в этой сессии не запускались ни разу — ни движок, ни
харнесс. Список живёт в `hooks.PreToolUse` определения `.claude/agents/`, это
конфигурация прав шага, и правит её оркестратор (запись 0018); плюс
определения замерзают на старте сессии, так что правка не помогла бы и ей.
Написанный, но ни разу не запущенный shell — главный риск этого PR, и он назван
в его теле отдельным абзацем.

## Изменённые файлы

| Файл | Зачем |
|---|---|
| `pipeline/route.json` | политика маршрута: пять позиций на трёх уровнях, `never_reduced` из пяти пунктов, `consumers`, `applied_by`, `decision` |
| `scripts/route.sh` | движок: `level` печатает маршрут уровня, `check` сверяет политику с деревом |
| `scripts/route.test.sh` | харнесс: 16 сценариев, из них 13 отрицательных на временных политиках и временных копиях потребителей (шестнадцатый добавлен починкой круга 2) |
| `.github/workflows/ci-fast.yml` | два шага в работу «Проверки обвязки»: `route.sh check` и `route.test.sh` |
| `scripts/protected-paths.sh` | шаблон `pipeline/.+` в `PROTECTED_PATTERNS` |
| `.github/CODEOWNERS` | строка `/pipeline/` в разделе «Сам пайплайн» |
| `pipeline/risk.json` | `note` сигнала `app-configuration`: утверждение о незащищённости `pipeline/**` стало ложным |
| `scripts/risk-score.test.sh` | тот же текст комментарием в сценарии; сценарии харнесса не менялись |
| `docs/decisions/0020-route-by-risk-level.md` | шесть расхождений с ТЗ и охват `pipeline/**` гейтом |
| `docs/decisions/README.md` | строка реестра для 0020 |
| `docs/decisions/0019-risk-config-json-not-yaml.md` | «Закрыто записью 0020» в разделе «Защита файла» |
| `docs/pipeline.md` | стрелка mermaid с условием, абзац в «Как апрувить план», абзац к таблице потолков, строка в реестре сверок, оговорки к обоим пунктам про авто-мердж |
| `docs/rules/plan.md` | безусловное утверждение о плановом круге заменено отсылкой к политике |
| `docs/rules/review-correctness.md` | то же в разделе «Предмет ревью — план» |
| `docs/rules/fix.md` | потолок кругов стал функцией уровня, отсылка на 0016 и 0020 |
| `docs/tasks/113.md` | журнал задачи |

Семнадцатый файл плана — `.claude/CLAUDE.md`, `owner: orchestrator`. Шаг
реализации к нему не прикасался: раздел «Маршрут по уровню риска» и
`pipeline/**` в списке защищённых путей остаются за оркестратором.

## Проверено

Контракт приёмки — шесть пунктов в плане круга 1; таблица по пунктам
заполняется на реализации. Прогоны разбора и плана:

| Что запускалось | Исход |
|---|---|
| `gh issue view 113 --json labels` | на разборе три метки, `agent/allow-protected` отсутствует; после ответа владельца стоит |
| `gh issue view 71/95/103` | #71 открыта, #95 открыта, #103 закрыта |
| `bash scripts/plan.sh validate <план>` | «План сходится: нарушений нет», код 0 |
| `sed -n` по девяти адресам `файл:строка` из плана | все девять существуют и содержат то, что план им приписывает |
| `grep -n pipeline scripts/protected-paths.sh .github/CODEOWNERS` | ноль попаданий в обоих — `pipeline/**` не защищён |
| `bash scripts/risk-score.sh plan <план>` | `medium`, сигнал `app-configuration` на двух путях `pipeline/` |
| `bash scripts/plan.sh render <план>` | опубликовано комментарием, метка `plan/proposed` |
| `sed -n` по пяти адресам из замечаний круга 1 | все сошлись: `pipeline.md:615` и `:885` — авто-мердж не включён; `risk-score.test.sh:560-561` — текст дословно тот; реестр решений — 0001–0019 без пропусков; цитата про плановый круг — на `plan.md:122` |
| `bash scripts/plan.sh validate <план круга 2>` | «План сходится: нарушений нет», код 0, отметка «файлов в плане 17, ориентир — до 15» |
| `bash scripts/risk-score.sh plan <план круга 2>` | `medium` — уровень не изменился, маршрут задачи не сокращается |
| `gh api .../timeline` по событиям меток | `plan/approved` поставлена 13:01:14Z — между публикацией круга 1 (12:46:37Z) и вердиктом ревью (13:03Z), то есть одобряет круг 1 |

Прогоны реализации:

| Что запускалось | Исход |
|---|---|
| `dotnet build --no-restore -c Release` | «Сборка успешно завершена», предупреждений нет |
| `dotnet test --no-build -c Release` | пройдено 42, не пройдено 0, пропущено 0 |
| `dotnet format --verify-no-changes --no-restore` | код 0 |
| `bash scripts/rules-sync.sh` | код 0, «Правила, промпты, определения и указатель сходятся»: правил 8, промптов 5, определений 7 — три новые отсылки копиями формулировок `CLAUDE.md` не оказались |
| `bash scripts/wiring.sh` | код 0, «Обвязка сходится: каждая проверка подключена», скриптов 21, workflow 9; по `scripts/route.sh` и `scripts/route.test.sh` ни жалобы, ни освобождения |
| `bash scripts/guard.test.sh` | пройдено 28, провалено 0, пропущено 0 — шаблон `pipeline/.+` в списке защищённых путей ничего не сломал |
| `bash scripts/risk-score.test.sh` | пройдено 32, провалено 0, пропущено 0 — правка `note` конфига и комментария сценария поведение не изменила |
| `bash scripts/plan.test.sh` | пройдено 30, провалено 0, пропущено 0 — три исторических плана при `--at` ложных срабатываний не дали, хотя список защищённых путей читается из рабочего каталога, а не из закреплённой ревизии |
| `grep -nE 'plan-review\|pr-review\|rescore\|мердж\|авто-мердж\|пропускается\|обязателен\|CLAUDE\|pipeline\.md\|docs/rules' scripts/route.sh` | ноль совпадений, кроме `scripts/route.sh:69: CONFIG_REL='pipeline/route.json'` — расположения политики по умолчанию. Пункт `policy-one-file` |
| `grep -c 'pipeline/route.json'` по пяти потребителям | `.claude/CLAUDE.md` — 0, `docs/pipeline.md` — 4, три файла правил — по 1. Ноль ровно у файла, который правит оркестратор |
| `grep -nFf` двенадцатью значениями позиций по потребителям, движку и записи 0020 | ноль совпадений: дословных копий значений в дереве нет |
| `bash scripts/plan.sh validate pipeline/route.json` | 17 нарушений схемы плана и ни одного «не разбирается как JSON»: политика — законный JSON ровно с семью полями верхнего уровня. Единственный доступный шагу способ проверить разбор — `jq` ему не выдан |
| `bash scripts/risk-score.sh diff main HEAD` | `medium`: файлов 15, строк 1508; сигналы `app-configuration` (`pipeline/risk.json`, `pipeline/route.json`) и `diff-size` (файлов 15 при пороге 11). Пересчёт по диффу уровень задачи не изменил — сокращений PR не получает |
| `bash scripts/route.sh level low\|medium\|high` | шагу не выдано — прогнал оркестратор, см. таблицу ниже |
| `bash scripts/route.sh check` | то же |
| `bash scripts/route.test.sh` | то же |
| Чеки PR #114 (`gh pr view --json statusCheckRollup`) | «Гейт целостности» — FAILURE: на PR нет метки `agent/allow-protected`, а защищённых путей в диффе десять. «Проверки обвязки» — FAILURE: по построению первым падает шаг `bash scripts/route.sh check`, потому что ссылки в `.claude/CLAUDE.md` ещё нет, и он же обрывает работу до шага с харнессом. Различить это от дефекта в ни разу не запущенном движке шаг не может: `gh run view --log` ему не выдан. Остальные завершившиеся работы — SUCCESS |


Прогоны оркестратора — три команды, не выданные шагу реализации, и его же
часть плана (`.claude/CLAUDE.md`, `owner: orchestrator`):

| Что запускалось | Исход |
|---|---|
| `bash scripts/route.sh level low` | код 0. Сокращены три позиции поимённо — `plan-review-machine`, `plan-review-rounds`, `pr-review-rounds`; в списке «не сокращается никогда» пять пунктов. Пункты `three-routes` и `low-named` |
| `bash scripts/route.sh level medium` | код 0, «Сокращено на уровне medium: ни одной позиции» |
| `bash scripts/route.sh level high` | код 0. `pr-review-rounds` — «оба круга ревью PR идут всегда, даже когда первый не нашёл ни одного замечания»; `diff-rescore` — «послабления „пересчёт здесь не требуется“ нет»; `merge` — «мердж делает человек руками» |
| Попарное сравнение трёх выводов | различаются все три пары. В паре `medium`/`high` различие несёт `pr-review-rounds` — то есть работающая позиция, а не инертная строка мерджа. Это и был блокер круга 1 |
| `bash scripts/route.sh check` **до** правки `.claude/CLAUDE.md` | код 1, расхождение ровно одно — «нет ссылки на политику маршрута: .claude/CLAUDE.md». Предсказание шага сошлось буквально |
| `bash scripts/route.sh check` **после** правки | код 0, «Маршрут сходится: политика, потребители и запись решения на месте». Позиций 5, пунктов «никогда» 5, потребителей 5, сокращено на низком уровне 3; все пять потребителей — «ссылка есть, дословных значений нет». Пункт `single-source` |
| `bash scripts/route.test.sh` | пройдено 15, провалено 0, пропущено 0, код 0 — 12 сценариев отрицательные |
| `bash scripts/rules-sync.sh` повторно, после правки `.claude/CLAUDE.md` | код 0. Окно копий на новом разделе не сработало: отсылки в правилах шагов пересказом его формулировок не оказались |
| Чеки PR #114 после коммита `2a8c103` | «Проверки обвязки» — **pass** (44 с): у шага они были красные ровно из-за отсутствующей ссылки, и правка `owner: orchestrator` их закрыла. Красным остался «Гейт целостности» — на PR нет метки |
## Осталось

Код и правки закончены, круг 1 ревью закрыт починкой. Незакрытое:

**Круг 2 ревью PR.** Уровень `medium`, второй круг по усмотрению — но первый
чистым не был: безопасность дала `changes_requested`. Обе половины, а не одна:
состав ревью не сокращается ни на каком уровне. Здесь задача впервые идёт по
маршруту, который сама же вводит.

**Снятие черновика с PR** — действие человека: оно запускает Copilot и цикловое
ревью, и до него показывать нечего.

**Два замечания вынесены отдельными задачами, а не забыты.** #115 — риск-скор
не даёт сигнала на дифф с новым анонимным эндпоинтом: в `keywords` сигнала
`sensitive-area` нет `allowanonymous`, а `authoriz` совпадает с
`RequireAuthorization`, но не с `.AllowAnonymous()`. План прямо вынес пересмотр
сигналов за рамки — предмет #103, — и это другой сигнал и другое поле, то есть
попутное в стороне. #116 — правило 7 `.claude/CLAUDE.md` утверждает «анонимен
ровно один эндпоинт», а их два: у второго анонимность обоснована комментарием
в коде, но записи в `docs/decisions/`, которой требует то же правило, нет.

Осознанно вне плана и названо вслух, чтобы не читалось как недосмотр:

- маршрут в агентском цикле — цикл идёт по самому строгому варианту;
- выборка человека на `low` — журнала решений фазы 5 нет, а выдуманная
  случайность даёт невоспроизводимый маршрут. `low` без человека не остаётся:
  апрув плана в списке «не сокращается никогда»;
- правило ТЗ «советник может блокировать, но не одобрять» — кода не получает,
  записывается в 0020 как отложенное к фазе 4.

Два места, где план расходится с механикой по порядку событий, а не по ошибке:
`.claude/CLAUDE.md` отдан `owner: orchestrator` (валидатор этого не проверяет,
файл правит оркестратор), и `pipeline/route.json` помечен `protected: false`,
потому что защищённым `pipeline/**` становится этой же задачей.

## Починка по замечаниям круга 1

Чинилось не красное: все чеки PR на момент починки зелёные. Предмет — четыре
замечания ревью, которые владелец отдал в починку.

**Причина обоих major безопасности одна: политика описывала маршрут словами
позиции, не проверив, что останется от правила в крайнем случае.**

1. **`pr-review-rounds` на `low`.** Сокращение «один круг, второй по просьбе
   человека» было безусловным, и в единственном случае, ради которого пункт 2
   «не сокращается никогда» и написан, оно съедало второй взгляд: половина
   выписала блокер, починка его закрыла, смотреть починку некому. Оговорка,
   стоявшая на `medium` («чистый первый круг позволяет остановиться»), на `low`
   отсутствовала — то есть отсутствовала ровно там, где нужнее. Сокращение
   стало условным по форме `diff-rescore` на том же уровне: `changes_requested`
   его снимает, отдельной просьбы человека на второй круг не нужно. Отметка
   `reduced: true` осталась: сокращение не исчезло, оно перестало быть
   безусловным, а на `reduced` держатся и инвариант сверки «на низком уровне
   сокращена хотя бы одна позиция», и пункт приёмки `low-named` с тремя
   поимёнными позициями.
2. **`merge` на `low` и `medium`.** «Все чеки позеленели» вердикта ревью не
   содержит и содержать не может: апрувов требуется ноль (`docs/pipeline.md`,
   branch protection), обе половины ревью публикуются комментариями и в
   required-чеки не входят. То есть в день включения «Allow auto-merge»
   обещание «обе половины ревью PR» стало бы пустым — половины отработали бы,
   остановить мердж не смогли. Предусловие записано в значениях уровней, а не
   отложено до дня включения настройки: отложенное предусловие в этот день и
   забывается. `reduced` у позиции по-прежнему `false` на всех трёх уровнях —
   инертность позиции предусловие не меняет.

**Пункт 2 списка «не сокращается никогда» правился вслед за уровнем, и это не
расширение рамок.** Он утверждал: «уровень сокращает число кругов, а не состав
ревью». После условного сокращения это верно уже не буквально — нечистый первый
круг сокращение снимает, то есть число кругов тоже сокращается не всегда.
Утверждение, оставшееся от прежнего состояния политики, — тот же дефект того же
класса в том же файле, из-за которого правился сам уровень; вынести его в issue
значило бы закрепить в дереве текст, противоречащий позиции строкой ниже.
Механически это не ловится ничем: `check` сравнивает тексты, а харнесс читает
`never_reduced` как список строк для печати.

**Nit про адресата прогона — про потерянный ход, а не про текст.** Три файла
правил называли команду `bash scripts/route.sh level <уровень>`, не сказав, кто
её запускает; шаг, читающий свои правила, тратил ход на отказ хука. Теперь в
каждом из трёх сказано: движок запускает оркестратор или человек, шагу остаётся
файл политики.

**Nit про сценарий обвязки — сценарий проверял не то, что называл.** Поиск шёл
подстрокой `grep -qF`, поэтому закомментированный шаг workflow оставлял
сценарий зелёным: строка остаётся в файле дословно. Парная проверка на копии с
вырезанной строкой была тавтологией — не находилось то, что сценарий сам и
удалил. Теперь вызов ищется строкой шага (перед вызовом только пробелы и
необязательный `run:`, поэтому блочная форма `run: |` тоже считается
подключённой, а комментарий — нет), а пара идёт на копии, где шаг
закомментирован на месте; отдельная ветка ловит несостоявшуюся мутацию, иначе
она читалась бы как «комментарий сошёл за вызов».

**Что в починку не входило и почему.** Замечание 3 безопасности
(`pipeline/risk.json`: анонимный эндпоинт не даёт сигнала) владелец вынес
отдельной issue — другой сигнал и другое поле, попутное в стороне. Тело PR и
раздел «Осталось» выше устарели на коммиты оркестратора, и правит их он же:
починке там нечего искать, а два автора в одном абзаце дают следующий круг
замечаний.

**Прогоны починки:**

| Что запускалось | Исход |
|---|---|
| `dotnet restore` | «Все проекты обновлены для восстановления» |
| `dotnet build --no-restore -c Release` | «Сборка успешно завершена», предупреждений 0, ошибок 0 |
| `dotnet test --no-build -c Release` | пройдено 42, не пройдено 0, пропущено 0 |
| `dotnet format --verify-no-changes --no-restore` | код 0, вывод пуст |
| `bash scripts/rules-sync.sh` | код 0, «Правила, промпты, определения и указатель сходятся»: правил 8, промптов 5, определений 7 — дописанные в трёх правилах фразы про адресата прогона окно копий не задели |
| `bash scripts/wiring.sh` | код 0, скриптов 21, workflow 9; ни `route.sh`, ни `route.test.sh` не числятся неподключёнными или освобождёнными |
| `bash scripts/plan.sh validate pipeline/route.json` | 17 нарушений схемы плана и ни одного «не разбирается как JSON» — правленая политика осталась законным JSON ровно с семью полями верхнего уровня. Единственный доступный шагу способ проверить разбор: `jq` ему не выдан |
| `grep -nE` новым выражением сценария по `.github/workflows/ci-fast.yml` | по одному попаданию на вызов: `:263` (`route.sh check`) и `:270` (`route.test.sh`) — оба шага активны |
| тот же `grep -nE` по пробнику из четырёх форм строки | закомментированный `# run:` не совпал, блочная форма и вызов с хвостом ключей совпали: выражение отличает подключённый шаг от текста о нём |

**Движок и харнесс шагу починки не выданы** — тем же списком
`hooks.PreToolUse`, что и шагу реализации, — поэтому в сессии они не
запускались, а правки проверены иначе: разбор политики валидатором плана,
выражение сценария прогонами `grep`. Прогнала их работа «Проверки обвязки»
после пуша, и её лог прочитан отбором по признакам:

| Из лога работы (прогон `33789273848`) | Числа |
|---|---|
| `bash scripts/route.sh check` | «Маршрут сходится: политика, потребители и запись решения на месте»; позиций 5, пунктов «не сокращается никогда» 5, потребителей 5, сокращено на низком уровне позиций 3 — правка условного сокращения отметку `reduced` не потеряла |
| `bash scripts/route.test.sh` | пройдено 15, провалено 0, пропущено 0 — переписанный сценарий обвязки прошёл, то есть мутация «закомментировать шаг на месте» состоялась и закомментированный вызов подключённым не сошёл |
| Работа целиком | pass, 45 с. Все чеки PR зелёные: гейт целостности, обвязка, сборка-стиль-тесты, уязвимые пакеты, секреты, сообщения коммитов |
| Повторно после правки пункта `never_reduced` (прогон `33789656154`) | те же числа: «Маршрут сходится», позиций 5, «никогда» 5, потребителей 5, сокращено на низком 3; сценарии — пройдено 15, провалено 0. Работа pass, 47 с |

Порядок здесь обратный обычному — сначала пуш, потом первый прогон правки, — и
это цена той же границы команд, которая стоит у шага реализации: заменить
прогон обещанием нельзя, а выдать шагу команду он себе не вправе.

## Починка по замечаниям круга 2

Чинилось снова не красное: на момент починки красных и pending чеков на PR
нет, оба вердикта круга 2 — `approve`. Предмет — два nit'а из трёх, которые
владелец отдал в починку; оба в `scripts/route.test.sh`.

**Причина у обоих одна и та же: сценарий харнесса называл предметом не то, что
проверял.** Это тот же класс дефекта, ради которого весь PR и написан, — в
файле, который отличает работающую проверку от её видимости.

1. **У инварианта разбора не было отрицательной пары, то есть ветка отказа
   была недостижима.** Инвариант движка сравнивает число прочитанных записей с
   `.positions | length` и отказывает «Разбор политики разъехался»; войти в
   него может только значение с переводом строки внутри — законная строка JSON,
   которую проверка формы пропускает, потому что смотрит тип и непустоту.
   Мутации харнесса портили отсутствующие, пустые и нестроковые поля — ровно
   то, что ловит проверка формы, и ни одна такого значения не подставляла.
   Шапка харнесса при этом объявляет стандарт «у каждого инварианта здесь
   пара»: недостающая пара делала стандарт утверждением о самом себе.

   Новый сценарий ставит перевод строки в середину значения первой позиции и
   утверждает не только текст отказа, но и **число прочитанных записей**
   (`позиций + 1`). Число здесь и есть высказывание инварианта: без него
   сценарий зеленел бы и на записи, которую не разрезало, — то есть был бы
   такой же видимостью, как отсутствующая пара. Отдельная ветвь ловит
   несостоявшуюся мутацию (перевода строки в значении нет). Пара к прогону
   `level` — `check` на той же политике: разбор отказывает раньше разбора
   режима, поэтому «Маршрут сходится» не печатается ни при каком состоянии
   дерева.

2. **Сценарий «Оба прогона подключены» был привязан к глобальному исходу сверки
   обвязки.** `expect_status 0` на прогоне `wiring.sh` означал, что любое
   несвязанное нарушение в другом скрипте покраснит этот сценарий под чужим
   именем и уведёт починку в чужой файл. Предмет сценария — четыре
   `expect_no_output` про `route.sh` и `route.test.sh`, они и держат его.
   Убрать привязку целиком нельзя: на коде 2 (сверка не запустилась вовсе)
   вывод пуст, и четыре ожидания прошли бы вхолостую. Поэтому осталось ровно
   два утверждения о прогоне: код 2 — провал с явным сообщением, и якорь на
   состоявшийся прогон — строка «Скриптов:», которую `wiring.sh` печатает
   после всех стартовых отказов.

**Что в починку не входило.** Третий nit — счёт расхождений с планом в теле
PR («один сценарий сверх таблицы плана», их два) — правит владелец: ошибка в
его тексте, а два автора в одном абзаце дают следующий круг замечаний.
Круг 3 ревью не запрашивался: потолок записи 0016 — два круга на любом уровне,
дальше задача уходит человеку, и запрос третьего был бы требованием того, чего
в политике нет.
