# Задача #69 — токен-оптимизации #63 не доехали до локального клиента

**Стадия:** ревью
**Ветка:** `feat/69-reading-rules-and-copy-guard`
**PR:** #72 (черновик)
**Следующий шаг:** снять с #72 черновик и запустить ревью. Замечаний по
существу к моменту открытия PR не осталось; спорным местом ревью, скорее
всего, окажется порог десяти слов — обоснование замером в разделе «Решения».

- [x] разбор — границы ясны, вопросы заданы
- [x] план — согласован владельцем
- [x] реализация
- [ ] ревью
- [ ] починка

## Границы

Входит — три звена одной цепочки: правило, его видимость там, где его читают,
механическая защита от того, чтобы видимость сделали копией.

| Что | Файлы |
|---|---|
| Правило чтения объёмного вывода | `docs/rules/reading.md`, ссылки из пяти `.github/workflows/agent-*.yml`, строки в таблицах `.claude/CLAUDE.md` и `docs/rules/README.md` |
| Видимость оговорки о частоте команд | отсылка в разделе «Команды» `.claude/CLAUDE.md` |
| Запрет копий правил | проверка 6 в `scripts/rules-sync.sh`, сценарии в `scripts/rules-sync.test.sh` |
| Следствие: две существующие копии | `docs/rules/fix.md`, `docs/rules/review-security.md` |

Не входит:

- выбор модели и набора инструментов под шаг локально — #70;
- оптимизации агентского цикла в `.github/**` — сделаны в #63;
- разбиение `.claude/CLAUDE.md` на ядро и `docs/rules/` целиком — отложено
  владельцем при разборе #63;
- проверка копий **между** файлами правил (`docs/rules/**` ↔ `docs/rules/**`).
  Критерий приёмки говорит про `.claude/CLAUDE.md` ↔ `docs/rules/**`. Замечено
  попутно: шапка «Правила проекта — `.claude/CLAUDE.md`, читать из
  репозитория» стоит дословно в `triage.md`, `implement.md`, `plan.md` и
  `fix.md`. Считать это копией или нет — отдельная issue, не эта правка;
- пересказ, отличающийся словами: список запрещённых подавлений в
  `.claude/CLAUDE.md` и его пересказ в `docs/rules/review-correctness.md`.
  Порог 10 слов его не ловит, а снижать порог ради него — ложные тревоги на
  всём остальном.

## Вопросы и ответы

| Вопрос | Ответ | Кто ответил |
|---|---|---|
| Объём 13 файлов против ориентировочных 10 в пункте 3 триажа — брать одной цепочкой или делить на «текст правил» и «механическая проверка»? | Одной цепочкой | владелец |
| Порог проверки копий: окно 10 слов (2 файла, 4 окна, все — настоящие копии) или 8 слов (4 файла, 16 окон, одно из них — не правило, а описание назначения каталога в `README.md`)? | 10 слов | владелец |

## Решения

**Правило — одним файлом `docs/rules/reading.md`, а не россыпью по файлам
шагов.** Задача называла разнесение по существующим файлам шагов более дешёвым
вариантом, и в диффе это так. Отвергнуто: чтение объёмного вывода бывает на
всех пяти шагах, значит это пять копий одного правила — ровно то, что
запрещает пункт 3 этой же задачи. Проверка 6 сравнивает `CLAUDE.md` с
`docs/rules/**` и копию между двумя файлами правил не увидит, то есть PR
вводил бы проверку и нарушал её принцип одним диффом.

Следствие: ни у одного файла правил нет шага с именем `reading`. Проверка 3 в
`rules-sync.sh` этого и не требует — она идёт от промпта к правилам его шага,
а не наоборот. Ссылка поставлена во **все пять** промптов, хотя проверке 2
хватило бы одной: промпт, который файл не упоминает, — это шаг, чей агент
правило не прочитает.

**Проверка 6 — запрет копий, а не сверка копий.** Побайтовое сравнение двух
копий требует разметки блоков маркерами и превращает `.claude/CLAUDE.md` в
генерируемый файл; отвергнуто. Реализовано обратное: совпадение формулировки в
двух местах само по себе является нарушением и лечится заменой копии на
ссылку. Это убирает причину, а не последствие, и ровно этого и так требует
`docs/rules/README.md:9–10`.

**Порог — скользящее окно 10 слов.** Выбран по замеру на репозитории, а не по
вкусу:

| Длина окна | Что попадает |
|---|---|
| предложение целиком, ≥7 слов | 1 совпадение; переписанная пунктуация обходит проверку |
| 8 слов | 4 файла, 16 окон — включая дублированное описание назначения каталога в `README.md`, а это не правило |
| 10 слов | 2 файла, 4 окна — все четыре настоящие копии правил |

Критерий выбора: каждое попадание должно быть копией, с которой человек
согласится. Первое ложное попадание — это место, где заводится
маркер-исключение, а исключений задача не допускает.

**Нормализация — без свёртки регистра и без классов символов с кириллицей.**
Локально `gawk 5.0`, на `ubuntu-latest` awk — скорее `mawk`, где `tolower()`
на кириллице не делает ничего; многобайтовый символ внутри `[...]` в локали `C`
разваливается на байты. И то и другое дало бы проверку, которая в CI ведёт
себя иначе, чем локально. Поэтому многобайтовая пунктуация перечислена
литералами, ASCII-пунктуация снимается `tr` (побайтово безопасно: в UTF-8
многобайтовые последовательности не содержат байтов < 0x80), а регистр не
сворачивается вовсе — окно в 10 слов над скопированным абзацем совпадает и без
этого. Проверено: вывод под UTF-8 и под `LC_ALL=C` совпадает.

**Заголовки и блоки кода из сравнения выброшены.** Код — не формулировка
правила. Имя раздела — это то, чем на правило **ссылаются**, и наказывать за
правильную ссылку проверка не должна.

**Порядок работ внутри PR обратный порядку в задаче.** Задача называет линию
разреза «сначала текст правил, потом механическая проверка» — это порядок для
случая, если задачу делят на два PR. Внутри одного PR порядок обратный:
сначала работающая проверка 6, потом новый текст под ней как под живым
гейтом. `reading.md` пишется про стоимость хода — тот же предмет, что пункт 4
`implement.md`, а отсылка в «Команды» соседствует с формулировкой оттуда:
новый текст мог бы уронить новую проверку.

**Граница проверки названа честно.** Пересказ другими словами не ловится.
Механически это недостижимо, и делать вид, что достижимо, хуже, чем назвать
границу — поэтому она записана в шапке скрипта, а не только здесь.

## Изменённые файлы

| Файл | Зачем |
|---|---|
| `docs/rules/reading.md` | новое правило: отбор по признакам до чтения целиком |
| `.claude/CLAUDE.md` | строка «Любой шаг» в таблице правил; отсылка в разделе «Команды» на частоту `dotnet restore` и `dotnet format` |
| `docs/rules/README.md` | строка в таблице и абзац о том, почему у этого файла нет своего процесса |
| `.github/workflows/agent-triage.yml` | ссылка на правило: триаж почти целиком состоит из чтения |
| `.github/workflows/agent-plan.yml` | ссылка: раздел «Как устроено сейчас» требует читать существующий код |
| `.github/workflows/agent-implement.yml` | ссылка: вывод сборки и тестов бывает длинным |
| `.github/workflows/agent-review.yml` | ссылка в оба job: у них разные промпты, один на два не делится |
| `.github/workflows/agent-fix.yml` | ссылка в основной промпт починки; детектор флаки не тронут — он читает готовую выжимку |
| `scripts/rules-sync.sh` | проверка 6, раздел «ЧЕГО ЭТА СВЕРКА НЕ ЛОВИТ», вторая строка подсказки в итоговом блоке |
| `scripts/rules-sync.test.sh` | три сценария на проверку 6; абзац прозы в фикстуру — на таблице проверка не сработала бы никогда |
| `docs/rules/fix.md` | копия формулировки об остановке с объяснением заменена отсылкой |
| `docs/rules/review-security.md` | пункт 1 излагал правило 5 вместо ссылки на него — вопреки заявлению в шапке того же файла |

Пункт 2 `review-security.md` намеренно не тронут: он повторяет правило 6
формулировкой из восьми слов, порог десяти её не ловит. Правка не по критерию —
расширение задачи, а снижение порога ради неё — ложные тревоги на всём
остальном.

## Проверено

| Что | Результат |
|---|---|
| `bash scripts/rules-sync.sh` | зелёный, 8 файлов правил, 5 промптов |
| `bash scripts/rules-sync.test.sh` | 11 сценариев, провалено 0 |
| то же под `LC_ALL=C` | 11 сценариев, провалено 0 |
| `bash scripts/wiring.sh` | обвязка сходится |
| проверка 6 на состоянии `HEAD` до правок | ловит обе копии, exit 1 |
| проверка 6 на подсаженном в чистый файл абзаце из `CLAUDE.md` | ловит, exit 1 |
| сверка на каждом из пяти коммитов ветки | зелёная на всех |
| `GUARD_ALLOW_PROTECTED=1 bash scripts/guard.sh` | нарушений нет |
| тот же гейт без метки | красный, как и должен |
| `gitleaks git --redact` | no leaks found |
| YAML всех девяти workflow | `yaml.safe_load` проходит |

Два предпоследних прогона стоят здесь не для полноты: проверка, которая только
зеленеет, зеленела бы и будучи выключенной. Мутационно это подтверждено
отдельно — при выключенной проверке 6 падает ровно сценарий с копией, при
пороге в шесть слов — ровно сценарий про короткое совпадение.

Сборки и тестов .NET в диффе нет: правка целиком в правилах, промптах и одном
shell-скрипте с харнессом.

## Осталось

Ничего из критерия приёмки. За рамками задачи остались три вещи, каждая
названа в разделе «Границы»: #70, проверка копий между файлами правил и
пересказ другими словами.
