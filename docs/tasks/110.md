# Задача #110 — адрес гейта в определении шага ревью корректности

**Стадия:** реализация закончена, открыт черновой PR
**Ветка:** `feat/110-review-correctness-gate-address`
**PR:** #112 — черновой
**Следующий шаг:** метка `agent/allow-protected` на PR #112 — с задачи она не
переносится, без неё гейт целостности красный, ставит человек. Дальше —
снятие черновика человеком и круг ревью.

- [x] разбор — границы ясны, вопрос один: метка
- [x] план — согласован владельцем (круг 2, машинное ревью круга 1 — `approve`)
- [x] реализация — все четыре пункта контракта приёмки зелёные
- [ ] ревью
- [ ] починка

## Границы

Входит:

- `.claude/agents/step-review-correctness.md` — одно предложение в абзаце
  «Признак — автор, а не позиция в списке»: адрес механизма, закрывающего класс
  угрозы «подделка комментария с планом», и разграничение «авторство задачи
  против авторства комментария»
- `docs/tasks/110.md` — этот журнал

Не входит:

- `docs/rules/review-correctness.md` — то же утверждение там уже исправлено
  #109 (PR #111, мерж `eadf22f`); повторно не трогается
- `.github/workflows/agent-*.yml` — промпты цикла задача закрывает прямо
- набор инструментов шага и список разрешённых команд в его `hooks.PreToolUse`
- переформатирование соседних абзацев и любой попутный редакторский проход

## Вопросы и ответы

| Вопрос | Ответ | Кто ответил |
|---|---|---|
| Метка `agent/allow-protected` на задаче — ставить сейчас или при открытии PR | поставлена владельцем в ходе круга 1 плана. На PR понадобится своя: с задачи метка не переносится | владелец |
| Существуют ли адреса, которые задача называет | сверено прогоном: `agent-review.yml` — job `plan-correctness` 589, гейт `author_association` 611, шаг «Предмет ревью — план» 626, выборка 644; `agent-plan.yml` — гейт на авторе задачи, строка 48 | триаж, прогон в дереве |
| Есть ли в правилах и определениях форма «файл:строка» | ноль попаданий `grep -rnE '`[^`]+\.(md\|yml\|sh\|json):[0-9]+`' .claude/agents/ docs/rules/` — номера строк в текст правки не идут | оркестратор, прогон в дереве |

## Решения

**Правит оркестратор, а не `step-implement`.** Путь `.claude/agents/**`
защищён, `Edit` у шага есть, но человека в его сессии нет и метка согласия туда
не доставляется — запись
[0018](../decisions/0018-local-orchestrator-and-step-subagents.md). Та же схема,
что у правки указателя в #106.

**Номер строки в текст не переносится.** Задача цитирует
`agent-review.yml:644` и 611 как адреса для сверки, но в тексте называется job
и шаг: формы «файл:строка» ни в `.claude/agents/**`, ни в `docs/rules/**` нет,
а в #109 замечание Copilot с просьбой добавить номер отклонено как более
хрупкое издание чинимого дефекта.

**Перенос строки — предмет проверки, а не вёрстки.** `plan-correctness` обязан
лечь целиком в одну физическую строку, иначе второй `grep` приёмки даёт ноль на
верном по смыслу тексте. Проверяется прогоном команды приёмки дословно.

**Путь проверяется отдельной командой, а не заодно с токеном.** Замечание круга
1 машинного ревью плана: риск переноса назывался и для токена, и для пути
`.github/workflows/agent-review.yml` — 37 символов при вёрстке ~76, — а мера
закрывала только токен. В способе проверки пункта `new-address-named` появилась
четвёртая сверочная команда, поиск `agent-review.yml` по определению шага.
Названная, но не существующая мера хуже отсутствующей: реализация пошла бы с
уверенностью, что проверено.

**Базовые прогоны сняты до правки.** `bash scripts/rules-sync.sh` и
`bash scripts/wiring.sh` на ветке задачи до единой правки — оба код 0. Без
снимка «до» зелёный прогон после правки не отличается от зелёного прогона,
который был бы и без неё.

## Изменённые файлы

| Файл | Зачем |
|---|---|
| `.claude/agents/step-review-correctness.md` | Абзац «Признак — автор, а не позиция в списке»: вместо неверного адреса `agent-plan.yml` названы шаг «Предмет ревью — план» и job `plan-correctness`, и отдельно сказано, что гейт `author_association` доказывает авторство задачи, а не комментария |
| `docs/tasks/110.md` | Этот журнал |

## Проверено

| Пункт контракта | Что запускалось | Исход |
|---|---|---|
| `stale-address-gone` | `grep -n 'agent-plan.yml' .claude/agents/step-review-correctness.md` | пусто, код 1. До правки — одна строка, 57 |
| `new-address-named` | `grep -n 'plan-correctness' .claude/agents/step-review-correctness.md` | строка 57, токен целиком внутри строки. До правки — ноль попаданий |
| `new-address-named` (сверх критерия) | `grep -n 'plan-correctness' .github/workflows/agent-review.yml`; `grep -n 'Предмет ревью — план' .github/workflows/agent-review.yml` | 488 и 589; 461 и 626 — job и шаг существуют под названными именами |
| `new-address-named` (сверх критерия) | `grep -n 'авторство задачи' .claude/agents/step-review-correctness.md` | строка 60. До правки вхождений корня «авторств» ноль |
| `new-address-named` (сверх критерия) | `grep -n 'agent-review.yml' .claude/agents/step-review-correctness.md` | строка 58 — путь не разорван переносом |
| `rules-sync-green` | `bash scripts/rules-sync.sh` | код 0, «Правила, промпты, определения и указатель сходятся». Вывод совпал с базовым прогоном до правки посимвольно |
| `wiring-green` | `bash scripts/wiring.sh` | код 0, «Обвязка сходится: каждая проверка подключена». Вывод совпал с базовым прогоном |

Прогоны, не привязанные к пунктам контракта:

| Что запускалось | Исход |
|---|---|
| `git diff --stat` | один файл, 5 строк добавлено, 2 удалено — дифф не вышел за абзац |
| `bash scripts/plan.sh validate` | код 0 на обоих кругах плана |
| gitleaks | прогоняется хуком `pre-commit`, коммит прошёл |

Сборка и тесты не запускались: правка чисто текстовая, ни одного файла `.cs`
дифф не касается.

## Осталось

Метка `agent/allow-protected` **на PR** — с задачи она не переносится, ставит
человек.
