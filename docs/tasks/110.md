# Задача #110 — адрес гейта в определении шага ревью корректности

**Стадия:** разбор
**Ветка:** `feat/110-review-correctness-gate-address` — создана, пуста
**PR:** —
**Следующий шаг:** ждать метку `agent/allow-protected` на задаче — без неё
гейт целостности покрасит PR. Метку ставит человек, вопрос задан комментарием
триажа. Параллельно идёт круг 1 плана; правка выполняется оркестратором, не
шагом реализации.

- [x] разбор — границы ясны, вопрос один: метка
- [ ] план — согласован владельцем
- [ ] реализация
- [ ] ревью
- [ ] починка

## Границы

Входит:

- `.claude/agents/step-review-correctness.md` — одно предложение в абзаце
  «Признак — автор, а не позиция в списке»: адрес механизма, закрывающего класс
  угрозы «подделка комментария с планом», и разграничение «авторство задачи
  против авторства комментария»
- `docs/tasks/110.md` — этот журнал

Не входит:

- `docs/rules/review-correctness.md` — то же утверждение там уже исправлено
  #109 (PR #111, мерж `eadf22f`); повторно не трогается
- `.github/workflows/agent-*.yml` — промпты цикла задача закрывает прямо
- набор инструментов шага и список разрешённых команд в его `hooks.PreToolUse`
- переформатирование соседних абзацев и любой попутный редакторский проход

## Вопросы и ответы

| Вопрос | Ответ | Кто ответил |
|---|---|---|
| Метка `agent/allow-protected` на задаче — ставить сейчас или при открытии PR | ждёт ответа владельца | — |
| Существуют ли адреса, которые задача называет | сверено прогоном: `agent-review.yml` — job `plan-correctness` 589, гейт `author_association` 611, шаг «Предмет ревью — план» 626, выборка 644; `agent-plan.yml` — гейт на авторе задачи, строка 48 | триаж, прогон в дереве |
| Есть ли в правилах и определениях форма «файл:строка» | ноль попаданий `grep -rnE '`[^`]+\.(md\|yml\|sh\|json):[0-9]+`' .claude/agents/ docs/rules/` — номера строк в текст правки не идут | оркестратор, прогон в дереве |

## Решения

**Правит оркестратор, а не `step-implement`.** Путь `.claude/agents/**`
защищён, `Edit` у шага есть, но человека в его сессии нет и метка согласия туда
не доставляется — запись
[0018](../decisions/0018-local-orchestrator-and-step-subagents.md). Та же схема,
что у правки указателя в #106.

**Номер строки в текст не переносится.** Задача цитирует
`agent-review.yml:644` и 611 как адреса для сверки, но в тексте называется job
и шаг: формы «файл:строка» ни в `.claude/agents/**`, ни в `docs/rules/**` нет,
а в #109 замечание Copilot с просьбой добавить номер отклонено как более
хрупкое издание чинимого дефекта.

**Перенос строки — предмет проверки, а не вёрстки.** `plan-correctness` обязан
лечь целиком в одну физическую строку, иначе второй `grep` приёмки даёт ноль на
верном по смыслу тексте. Проверяется прогоном команды приёмки дословно.

## Изменённые файлы

| Файл | Зачем |
|---|---|
| | |

## Проверено

| Пункт контракта | Что запускалось | Исход |
|---|---|---|
| | | |

## Осталось

Пусто — работа только началась.
