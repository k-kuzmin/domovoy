# Задача #81 — согласие из issue до субагента не доезжает: кто правит конфигурацию прав шага

**Стадия:** реализация
**Ветка:** `feat/81-who-edits-step-permissions`
**PR:** —
**Следующий шаг:** ревью — снять черновик с PR

- [x] разбор — границы ясны, вопросы заданы
- [x] план — согласован владельцем
- [x] реализация
- [ ] ревью
- [ ] починка

## Границы

Входит:

- `docs/decisions/0018-local-orchestrator-and-step-subagents.md` — дополнение
  либо новая запись рядом (решает владелец);
- `.claude/CLAUDE.md`, раздел «Локальный режим: оркестратор и шаги» — если
  решено сказать то же и там, то ссылкой на запись, а не копией.

Не входит:

- `scripts/step-bash-allow.sh` и сам классификатор прав локальной сессии —
  наблюдение о поведении, а не заявка на его изменение;
- рамки определений шагов — закрыты в #78;
- правка самих `.github/workflows/agent-*.yml`. Вопрос «кто вправе расширять
  `--allowedTools` в цикле» в объём вошёл, но как раздел записи: если механизм
  цикла уже отвечает на него, запись это фиксирует; если нет — расхождение
  называется в PR.

## Вопросы и ответы

| Вопрос | Ответ | Кто ответил |
|---|---|---|
| Нужна ли метка `agent/allow-protected` | да, поставлена | владелец |
| Дополнить 0018 или завести отдельную запись | дополнить 0018 — наблюдение внутри той же схемы, отдельная запись разорвала бы мысль | владелец |
| Говорить ли то же в `.claude/CLAUDE.md` | да, пятым пунктом перечня «что оркестратор делает сам», ссылкой на запись | владелец |
| Входит ли в объём цикл (`agent-*.yml`) | да — запись отвечает на вопрос разделом; правка самих `agent-*.yml` не подразумевается | владелец |

## Решения

**Реализация ведётся в отдельном git worktree** — по просьбе владельца в
сессии. На результат это не влияет: ветка, коммиты и PR те же, изолируется
рабочее дерево. Практическая причина назвать это здесь — в основном дереве
лежит неотслеживаемый журнал чужой задачи (`docs/tasks/83.md`), и отдельное
дерево гарантирует, что он не попадёт в дифф #81.

Все четыре развилки закрыты владельцем до начала правок — комментарий
[#81 (comment)](https://github.com/k-kuzmin/domovoy/issues/81#issuecomment-5386458179),
ответы в таблице выше. Долгоживущая часть уезжает дополнением к записи
[0018](../decisions/0018-local-orchestrator-and-step-subagents.md).

**Правило записано через отсутствие человека в сессии, а не через поведение
классификатора.** Классификатор — свойство версии CLI: его сообщение об отказе
может измениться или исчезнуть, и правило не должно устареть вместе с ним.
Наблюдение #78 стоит в записи как то, чем правило обнаружено. Отвергнуто:
формулировка «правку отбивает классификатор, поэтому её делает оркестратор» —
она описывает инструмент, а не схему.

**Вопрос про цикл закрыт описанием механизма, а не правкой `agent-*.yml`** —
как и решил владелец. Три факта проверены в этой сессии: `--allowedTools` шагов
реализации и починки содержат `Edit`/`Write`, но ни `gh issue edit`, ни
`gh pr edit` (`agent-implement.yml:133`, `agent-fix.yml:496`); разрешение гейта
берётся из меток **PR** (`ci-fast.yml:162-163`), сам гейт подключён
(`ci-fast.yml:172`), а `.github/.+` входит в `PROTECTED_PATTERNS`
(`guard.sh:244`). Отсюда формулировка записи: локально граница
предупредительная, в цикле посмертная. Номера строк остались здесь, в записи
их нет намеренно — 0018 нигде не ссылается номерами, они снимок момента.

**Пятый пункт перечня в `.claude/CLAUDE.md` начат с оговорки «причина здесь
иная».** Перечень озаглавлен «что оркестратор делает сам, потому что субагенту
это не выдано», а `Edit` субагенту как раз выдан — без оговорки пункт
противоречил бы собственному заголовку.

## Изменённые файлы

| Файл | Зачем |
|---|---|
| `docs/decisions/0018-local-orchestrator-and-step-subagents.md` | новый раздел «Кто правит конфигурацию прав шага» с подразделом про цикл; одна отвергнутая альтернатива — отдельная запись рядом |
| `.claude/CLAUDE.md` | пятый пункт перечня «что оркестратор делает сам» — правило и ссылка на 0018, без пересказа обоснования |
| `docs/tasks/81.md` | журнал задачи |

## Проверено

Триаж (`step-triage`) — `ready: false`: содержание годное, не хватало метки
`agent/allow-protected` на защищённые пути. Метка поставлена, вопрос снят.

`scripts/rules-sync.sh` до правок — exit 0. Базовая точка зафиксирована, чтобы
позднее падение было отнесено к правкам этой задачи, а не унаследовано.

После правок:

| Проверка | Результат |
|---|---|
| `bash scripts/rules-sync.sh` — сразу после правки `CLAUDE.md`, до правки записи | 8 файлов правил, 5 промптов, 7 определений, расхождений 0 |
| `bash scripts/rules-sync.sh` — после правки записи | расхождений 0 |
| `bash scripts/rules-sync.test.sh` | все проверки пройдены |
| `dotnet build --no-restore -c Release` | ошибок 0, предупреждений 0 |
| `dotnet test --no-build -c Release` | 42 из 42, пропущено 0 |
| `dotnet format --verify-no-changes --no-restore` | без правок |
| `gitleaks` через `pre-commit` | чисто |

Правку прозы в защищённых файлах классификатор не отбил — сходится с
наблюдением #78: блокируется расширение списка разрешённых команд, а не путь.

## Осталось

Ревью. Черновой PR открыт; снимает черновик человек.

**Предусловие, которое шаг закрыть не может:** `agent/allow-protected` стоит на
issue, а `scripts/guard.sh` читает метки PR. До того как метка появится на PR,
проверка 1 гейта целостности в `CI (fast)` красная — оба изменённых файла
защищены. Метку ставит человек: `gh pr edit` выдан шагу только под тело PR,
правка меток им запрещена.

Отдельно, **не в этом диффе:** план нашёл устаревшую формулировку про
`scripts/guard.sh` («появляется следующим шагом внедрения пайплайна») в
`.claude/CLAUDE.md` и `.github/CODEOWNERS:15` — скрипт существует и подключён
в `ci-fast.yml:172`. Уходит отдельной issue.
