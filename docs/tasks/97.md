# Задача #97 — машиночитаемый план и детерминированный валидатор

**Стадия:** оба ревью пройдены, вердикты `approve`
**Ветка:** `feat/97-machine-readable-plan` — в `origin`
**PR:** #99 (черновик)
**Следующий шаг:** решение владельца по четырём major — чинить в этом PR или
выносить отдельными задачами. Блокеров нет ни у одного ревью, по
`docs/rules/README.md` major мержу не препятствует. После решения — либо
`step-fix` по выбранным пунктам, либо снятие черновика.

**Метка `agent/allow-protected` на PR #99 поставлена владельцем**, гейт
целостности позеленел без close+reopen — он читает метки PR, а метка легла до
следующего пуша, и новое событие пришло с ним само. Проверок двенадцать, все
проходят: десять `pass`, два (`CodeQL`, «Стек поднимается на примерах») по
условию пропускаются. Номер головы здесь не пишется намеренно — он устаревает
следующим же коммитом, состояние смотреть `gh pr checks 99`.

Оркестраторская часть, сделанная после шага, — от `2002285` до `d3be1e5`.
Содержательных коммитов три, остальные три ведут этот журнал:

- `2002285` — `.claude/agents/step-plan.md` (возврат JSON по схеме вместо
  прозы) и `.claude/agents/step-review-correctness.md` (режим «предмет ревью —
  план», различение объекта по `url`). `tools` и списки команд у обоих не
  тронуты;
- `bd5252c` — строка в «Правилах синхронизации» `.claude/CLAUDE.md`, которую
  план отдавал реализации;
- `2c7b4bf` — вторая правка `.claude/CLAUDE.md`, **сверх плана**: пункт
  «публикует наружу» раздела «Локальный режим» называет порядок `validate` →
  `render` → публикация. Обоснование — в «Решениях».

После каждой из трёх `scripts/rules-sync.sh` — код 0; `rules-sync.test.sh` —
25 из 25, `wiring.sh` — код 0 на 15 скриптах.

**Правку `.claude/CLAUDE.md` оркестратор с первого хода не провёл:**
классификатор auto-режима отказал и через `Edit`, и через оболочку. Обход не
делался — гейт стоит по делу; обе строки поставлены после явного хода
владельца. Запись оставлена, потому что порядок повторится на каждой задаче,
которая трогает этот файл.

Правка числа «шесть» в разделе «Тесты» тела задачи сделана раньше, коммитом
`5ebc2b4`; в теле остаётся «шестью» в разделе «Оценка» (`:89`) — это оценка
трудоёмкости, а не критерий приёмки, и на приёмку не влияет. Статус доски не
выставлялся ни разу: `scripts/set-project-status.sh` требует токен со scope
`project`, которого у локальной сессии нет.

План опубликован
([#issuecomment-5443008699](https://github.com/k-kuzmin/domovoy/issues/97#issuecomment-5443008699))
с двумя поправками — вынос `guard.sh`
([#issuecomment-5443133334](https://github.com/k-kuzmin/domovoy/issues/97#issuecomment-5443133334))
и цикловая половина
([#issuecomment-5443827047](https://github.com/k-kuzmin/domovoy/issues/97#issuecomment-5443827047)).

Ответы владельца —
[#issuecomment-5442786805](https://github.com/k-kuzmin/domovoy/issues/97#issuecomment-5442786805),
вердикт триажа —
[#issuecomment-5442665634](https://github.com/k-kuzmin/domovoy/issues/97#issuecomment-5442665634).

- [x] разбор — вердикт «не готова», четыре вопроса заданы, ответы получены
- [x] план — согласован владельцем, `plan/approved`
- [x] реализация — PR #99, черновик; оркестраторская часть в `2002285`, `bd5252c`, `2c7b4bf`
- [x] ревью — оба `approve`; корректность 3 major + 2 nit, безопасность 1 major + 2 nit
- [x] починка — семь замечаний закрыты, major про опознание плана закрыт
  оркестратором до неё; повторный круг ревью не запрошен — это за оркестратором

## Границы

Входит: схема машиночитаемого плана, детерминированный валидатор в `scripts/`
с парным `*.test.sh`, интеграция в оба места публикации плана
(`.github/workflows/agent-plan.yml` и `.claude/agents/step-plan.md`), модельный
круг ревью плана в локальном режиме — `docs/rules/review-correctness.md`,
`.claude/agents/step-review-correctness.md`, `docs/pipeline.md`,
`docs/rules/plan.md`.

Не входит: риск-скоринг, мутационное тестирование, сам советник, триггер
планового круга ревью в цикле (`agent-review.yml` запускается по событиям PR,
события на `plan/proposed` там нет — уходит отдельной задачей).

## Вопросы и ответы

| Вопрос | Ответ | Кто ответил |
|---|---|---|
| Метка `agent/allow-protected` — поставить | За владельцем, агент себе гейт не ставит. На момент записи метки нет | владелец |
| Граница PR: только валидатор или сразу и модельный круг ревью плана | **Один PR.** Размер выше ориентировочных десяти файлов принят осознанно | владелец |
| Искусственных планов шесть или семь — классов нарушений стало семь | **Семь**, по одному на класс, включая различение `непроверяем` и пустой ячейки. Тело задачи владелец поручил поправить оркестратору: раздел «Тесты» читает «на семи», рядом названы седьмой класс и ручной перенос реальных планов | владелец |
| «Реальные планы из истории» как экземпляры схемы — перенос в объёме или критерий `непроверяем` | **Два-три плана переносятся вручную, перенос в объёме.** Вариант «признать `непроверяем`» отвергнут: дыра в критерии дороже трёх файлов ручной работы | владелец |
| Вынос `PROTECTED_PATTERNS` из `guard.sh` — кто делает: реализация, оркестратор или не делать вовсе | **Оркестратор**, рядом с четырьмя определениями. Задан после публикации плана, поправка опубликована отдельным комментарием | владелец |

Пришло готовым из #93 и переспрашиванию не подлежит (комментарий владельца от
25.08): контракт приёмки живёт в плане; `непроверяем` — значение схемы, а не
отсутствие поля; контракт обязателен всегда; контракт проходит ревью
корректности, отдельного шага приёмки нет.

## Решения

**Разбиение на две issue не предлагается.** Владелец отклонил его явно
(комментарий от 26.08) с причиной: разделение труда «что ловит скрипт, а что
остаётся чтению» иначе решается дважды и расходится дырой или дублем. Причина
принята; вопрос владельцу переформулирован на границу PR — разделение труда
решается планом этой задачи один раз, реализация может уехать двумя PR.

**Вопрос про лимит размера плана снят с триажа.** Тело задачи само отдаёт его
планированию. Вердикт шага держал его блокирующим — поправлено: готовности
задачи он не мешает.

**Два расхождения с критерием приёмки добавлены оркестратором.** Шаг их не
нашёл: классов нарушений семь, а не шесть (седьмой — различение `непроверяем`
и пустой ячейки, обязательное с 25.08 и названное хвостом в журнале #93), и
машиночитаемых планов в истории задач не существует ни одного, поэтому вторая
половина критерия невыполнима как записана, а не трудна.

**Правку `.claude/agents/step-plan.md` и `step-review-correctness.md` делает
оркестратор.** `docs/rules/plan.md:25-31` запрещает поручать конфигурацию прав
шага реализации, и запрет покрывает тело определения, не только `tools` и
список команд. Обоснование —
[0018](../decisions/0018-local-orchestrator-and-step-subagents.md).
Планирование добавило к этим двум ещё два определения — `step-implement.md` и
`step-fix.md`, которым нужны в списке хука `bash scripts/plan.sh` и
`bash scripts/plan.test.sh`. Эти две обязаны лечь до старта реализации.

**Вторая правка `.claude/CLAUDE.md` — сверх плана, и это расхождение.** План
называл одну строку в «Правилах синхронизации». Найдено при сверке контракта
приёмки после реализации: пункт «комментарий формируется из структуры» в цикле
доказан нулём вхождений `plan_markdown` в workflow, а локально держался на том,
что оркестратор помнит порядок «`validate` → `render` → публикация». Ни
`.claude/CLAUDE.md`, ни `docs/pipeline.md` этого порядка адресно не называли:
pipeline описывает его безлично, а список «Что оркестратор делает сам» говорил
просто «публикует план». Инструкция, живущая в определении шага, оркестратору
не видна — он этот файл не читает. Мера: оговорка в том самом пункте.
Альтернатива «дописать только в `docs/pipeline.md`» отвергнута владельцем:
`.claude/CLAUDE.md` — единственный файл, который оркестратор читает всегда.

**Вынос `PROTECTED_PATTERNS` из `scripts/guard.sh` — тоже за оркестратором**,
решение владельца после публикации плана. Итого шесть правок за оркестратором.
`docs/rules/plan.md:25-31` этого не требует — там названа конфигурация прав шага,
а не скрипт гейта, — но причина оттуда переносится дословно: `guard.sh` есть
механизм, заменяющий человека в сверке защищённых путей, и поручать его правку
шагу, чьи правки он же полицейскит, одним диффом с проверяемым — форма из
открытой #87. Мера из риска 6 остаётся: `guard.test.sh` до и после, 28/0/0.

### Решения планирования (полностью — в комментарии с планом)

**«Как устроено сейчас» получает поле схемы**, а не остаётся прозой рядом:
человекочитаемый комментарий рендерится из структуры, значит раздел без поля
физически не попал бы в опубликованный план.

**Адресат правки становится полем `owner`** (`implement` | `orchestrator`) — это
названное расхождение с формулировкой доработки владельца «скрипт видит путь, не
видит адресата». Классов нарушений остаётся семь: адресат входит в класс 3.

**Лимит размера плана — предупреждение, не отказ**, порог 15 файлов от ориентира
«до 10» в `docs/rules/triage.md:28-30`. Жёсткий предел краснел бы на плане,
который человек уже одобрил.

**Самоопределение `step-review-correctness` по типу объекта принято, но не в
предложенной форме.** Наивный различитель «вызов `gh issue view` удался — значит
issue» отвергнут измерением: `gh issue view 98` успешно возвращает **PR**.
Различение по сегменту `url` (`/pull/` против `/issues/`) — однозначно, команда
шагу уже разрешена.

**Новый файл правил заводить нельзя** — проверка 2 `scripts/rules-sync.sh`
требует упоминания файла правил и промптом `agent-*.yml`, и определением
`step-*.md`, а процесса планового круга в цикле нет. Режим ревью плана живёт
разделом в `docs/rules/review-correctness.md`.

**Список защищённых путей выносится в `scripts/protected-paths.sh`** и
источается `guard.sh` и валидатором: своя копия в валидаторе была бы четвёртым
описанием одного множества.

**Записи в `docs/decisions/` не заводится**: обоснование механических сверок в
этом репозитории живёт в шапке скрипта, как у `wiring.sh` и `rules-sync.sh`.

**Цикловая половина проверена отдельным вопросом владельца** и оказалась
полной по замыслу, но неполной по описанию: план назвал замену аргумента
`--json-schema` и не назвал текст промпта `agent-plan.yml`, который сегодня
требует «план текстом». Второе — две команды для шагов реализации и починки
цикловой половины не имеют **по построению**: ни один шаг цикла не пускает
`bash scripts/*.sh` вовсе (проверено по всем `--allowedTools`), харнессы в цикле
гоняет CI после пуша. Сверка правил содержимое списков команд не сравнивает
(открытая #82), поэтому расхождение названо в поправке вслух, а не оставлено
механике. Отложенный триггер планового круга в цикле подтверждён деталью: `gh
issue view` в границе ревью цикла уже разрешён (`agent-review.yml:227`), не
хватает ровно события.

Записано заранее как ожидаемое место отказа: `--max-turns 25` у планирования
(`agent-plan.yml:90`) — чтение схемы добавляет ходов. Потолок вслепую не
правится.

**Плановый круг ревью к этому плану не применялся.** Механизм — предмет самой
задачи: `docs/rules/review-correctness.md` режима «предмет ревью — план» пока не
содержит, а определение шага говорит про дифф PR. Вызов шага с номером issue
сейчас дал бы поведение, не описанное ни одним правилом. Вместо круга несущие
факты плана перепроверены оркестратором вручную — список в комментарии с планом,
раздел «Сверка оркестратора перед публикацией».

### Решения реализации

**Разделитель полей при разборе вывода `jq` — U+001F, не табуляция.** Найдено
падением сценария класса 7: табуляция входит в IFS-пробелы, `read` схлопывает
две подряд идущие в одну, и пустая ячейка `method` посреди строки сдвигала все
следующие поля влево — валидатор докладывал «нет доказательства» вместо «нет
способа проверки». То есть ровно на том случае, ради которого класс 7 и
написан. В программах `jq` разделитель собирается через `implode`, чтобы в
исходнике не лежал управляющий байт.

**Все вызовы `jq` идут через обёртку `pjq`, снимающую CR.** На Windows `jq`
отдаёт CRLF, и `scripts/**` с хвостовым возвратом каретки не совпадает ни с
одной границей: первый прогон дал четыре ложных «путь вне границ задачи» и два
ложных «пункт не покрыт тестом». На Linux `tr` не меняет ничего, поэтому
обёртка одна на обе платформы, а не ветка по системе.

**Форма проверяется `--slurpfile`, а не `--argjson`.** План и схема целиком в
аргументе упираются в предел длины командной строки: проверка формы отказывала
бы ровно на больших планах. Найдено на первой же исторической фикстуре.

**Рендер берёт заголовки и порядок разделов из схемы, а не из списка внутри
скрипта.** Иначе сценарии «порядок разделов» и «инвариант рендера» проверяли бы
копию. Поле схемы, до которого рендер не дошёл, роняет рендер кодом 2 — молчать
о потерянном разделе нечем.

**Поиск существующих тестов исключает сами планы** (`scripts/fixtures/plans`,
`scripts/plan.test.sh`). Без этого любой план, называющий новый тест по имени,
краснел бы классом 5 о собственный текст, лежащий в репозитории. Названо в
шапке скрипта, раздел «ЧЕГО НЕ ЛОВИТ».

**Пункт приёмки со значением `непроверяем` освобождён от проверки 4.** Теста у
него нет по определению. Это способ обойти класс 4, и он назван вслух: причина
при `непроверяем` обязательна, читает её человек.

### Расхождения с планом

1. **Строка в «Правилах синхронизации» `.claude/CLAUDE.md` не сделана.** План
   отдавал её реализации и называл «пятым файлом сверх четырёх». Собственная
   конфигурация шага запрещает править `.claude/CLAUDE.md`: сообщение
   оркестратора с номером задачи согласием человека не является, апрув плана в
   issue — не тот канал. Строка уходит оркестратору седьмой правкой.
2. **Появился `scripts/fixtures/plans/README.md`, которого в плане нет.** План
   требовал записать факт восстановления контракта «в шапке фикстуры»; у JSON
   шапки не бывает, а поле под примечание расширяло бы схему ради одной
   надобности. Файл лежит в подкаталоге и сверку обвязки не задевает — она
   смотрит `*.sh` на одном уровне.
3. **Сценарий «без `--at` исторический план краснеет» идёт на #83, а не на
   #93.** Проверено: #93 без `--at` зелёный — все его пути дожили до сегодня, а
   тестов с `new: true` у него нет вовсе. #83 объявляет новыми ровно те
   сценарии, которые с тех пор появились в дереве, и разница деревьев на нём
   видна тем самым классом 5.
4. **Сценариев в харнессе 23, а не восемнадцать строк таблицы плана.** Добавлены
   три сценария формы (отсутствующее обязательное поле, поле не по схеме,
   значение вне `enum`) и два запуска-отказа (неразрешимый ref, план не
   разбирается как JSON). Ни один сценарий плана не выброшен.
5. **`gitleaks` отдельной командой не запускался** — его нет в списке команд
   шага. Отработал `pre-commit` на каждом из четырёх коммитов; отказов не было.

## Изменённые файлы

| Файл | Зачем |
|---|---|
| `docs/tasks/97.md` | этот журнал |
| `scripts/plan-schema.json` | схема машиночитаемого плана: семь разделов полями, `boundaries`, `flags`, `owner`, `protected`, связь `tests[].covers → acceptance[].id`, заголовки разделов в `x-title` и `x-section`. Единственный источник формы для шага, валидатора и харнесса. Коммит `469241e` |
| `scripts/plan.sh` | `validate` (форма по схеме + семь классов + предупреждение о размере) и `render` (markdown разделами `plan.md`), флаг `--at <ref>`. Коммит `469241e` |
| `scripts/plan.test.sh` | 28 сценариев: согласованный план, семь классов, форма, полнота фикстуры, инвариант рендера, порядок разделов, размер, зависимость, три исторических плана, пины, отказы запуска. Коммит `7b8d93c`, при починке добавлены пять — раздел «Починка» |
| `scripts/fixtures/plans/93.json`, `83.json`, `81.json` | реальные планы из истории, перенесённые вручную. Коммит `7b8d93c` |
| `scripts/fixtures/plans/README.md` | что перенесено, что восстановлено и чего фикстуры не проверяют — вместо шапки, которой у JSON не бывает. Коммит `7b8d93c` |
| `.github/workflows/agent-plan.yml` | `plan_markdown` → `plan_json`, текст промпта под схему, публикация через `validate` и `render`, JSON свёрнутым блоком, красный путь без `plan/proposed`. Коммит `30727e0` |
| `.github/workflows/ci-fast.yml` | шаг «Сценарии валидатора плана» и `fetch-depth: 0` у checkout работы «Проверки обвязки». Коммит `30727e0` |
| `docs/rules/plan.md` | раздел «Форма — машиночитаемая»: схема, валидатор, семь классов, предупреждение о размере, поля `protected` и `owner`; круг ревью плана до апрува. Коммит `0ea61db` |
| `docs/rules/review-correctness.md` | раздел «Предмет ревью — план»: различение по `url`, пять пунктов чтения, «чего этот круг не ловит». Коммит `0ea61db` |
| `docs/pipeline.md` | ребро `plan/proposed` → круг ревью плана в mermaid, строка в таблице сверок, расхождение режимов названо. Коммит `0ea61db` |
| `scripts/protected-paths.sh` | список защищённых путей вынесен из `guard.sh`: потребителей стало два, а копия в валидаторе была бы четвёртым описанием одного множества. Оркестратор, коммит `5a18c0f` |
| `scripts/guard.sh` | источает список вместо своей копии; путь считается от каталога скрипта, потому что гейт зовут и из корня, и по абсолютному пути из харнесса. Добавлена страховка: пустой или ненайденный список — код 2, а не молчаливый пропуск, потому что `set -e` здесь намеренно выключен. Оркестратор, коммит `5a18c0f` |
| `.claude/agents/step-implement.md`, `.claude/agents/step-fix.md` | `bash scripts/plan.sh` и `bash scripts/plan.test.sh` в списки хука: без них шаг не запустит собственный харнесс. Оркестратор, коммит `50d73bc` |
| `.claude/agents/step-plan.md` | `description` и контракт возврата: JSON по `scripts/plan-schema.json` вместо текста, параллельной прозы не пишется. Названо, что валидатор шагу не выдан намеренно — прогоняет оркестратор до публикации, и отказ возвращается шагу, а не владельцу. Оркестратор, коммит `2002285` |
| `.claude/agents/step-review-correctness.md` | `description` и второй предмет ревью — план: различение режима по сегменту `url`, откуда читать план и его машиночитаемую форму, и что проверенное валидатором не перечитывается. `tools` и список команд не тронуты: `gh issue view` и `Read` уже разрешены. Оркестратор, коммит `2002285` |
| `.claude/CLAUDE.md` | строка в «Правилах синхронизации»: схема плана ↔ ветка рендера в `plan.sh` ↔ раздел в `docs/rules/plan.md`. План отдавал её реализации, но правка этого файла запрещена конфигурацией шага. Оркестратор, коммит `bd5252c`. Второй правкой — оговорка в пункте «публикует наружу» раздела «Локальный режим»: локально порядок «`validate` → `render` → публикация» больше не подразумевается, а записан. Сверх плана, обоснование в «Решениях» |

## Проверено

Контракт приёмки — раздел 5 согласованного плана, тринадцать строк.

| Пункт контракта | Что запускалось | Исход |
|---|---|---|
| Вывод шага планирования машиночитаем | `grep '"properties"' scripts/plan-schema.json` и `bash scripts/plan.sh validate scripts/fixtures/plans/93.json --at f2ae89b` | в схеме есть `current_state`, `approach`, `files`, `boundaries`, `flags`, `tests`, `acceptance`, `risks`, `out_of_scope`; прогон на историческом плане — «План сходится: нарушений нет», код 0 |
| Комментарий формируется из структуры, а не пишется параллельно | `bash scripts/plan.sh render scripts/fixtures/plans/93.json` и `grep -c 'plan_markdown' .github/workflows/agent-plan.yml` | в рендере семь заголовков `## ` в порядке `plan.md`; вхождений `plan_markdown` — **0**, второго канала прозой не осталось. Локальная половина закрыта не тем же способом: в цикле порядок «`validate` → `render` → публикация» зашит в workflow, локально он записан в `.claude/CLAUDE.md`, пункт «публикует наружу» раздела «Локальный режим» — иначе он держался бы на памяти оркестратора, а это ровно та тихая расходимость, которую задача и закрывает |
| Валидатор детерминированный, без обращения к модели, до публикации | `grep -n 'plan.sh\|gh issue comment' .github/workflows/agent-plan.yml`, `grep -nE 'curl\|anthropic\|claude' scripts/plan.sh` | `validate` на строке 153, публикация комментарием — 206, то есть до; в скрипте ни `curl`, ни обращений к сети — единственное совпадение по `claude` это константа пути `.claude/agents/` |
| Список обязательного берётся из схемы, а не из скрипта | `grep -n 'required' scripts/plan.sh` | два вхождения: строка шапки и `($s.required // [])[]` в программе `jq`. Перечисления имён полей в проверке формы нет ни одного. У рендера имена перечислены неизбежно, и вторая половина правила стоит там: поле схемы без ветки рендера роняет рендер кодом 2 — сценарии «Полнота согласованной фикстуры» и «Инвариант рендера» |
| Семь классов нарушений на семи искусственных планах | `bash scripts/plan.test.sh` | семь сценариев «Класс N», каждый на своей точечной мутации согласованного плана, у всех код 1 и названное нарушение; итог на момент реализации **пройдено 23, провалено 0, пропущено 0**, после починки по замечаниям — 28 |
| Ни одного ложного срабатывания на реальных планах из истории | тот же прогон | три сценария на `{93,81,83}.json` с кодом 0 и пустым списком сообщений. Сужение остаётся названным: у #81 и #83 контракт восстановлен из таблиц «Проверено» их журналов — подробно в `scripts/fixtures/plans/README.md` |
| Проверка «путь не выходит за границы» | тот же прогон, сценарий класса 2 | код 1, в сообщении и путь, и полный список границ. Сужение прежнее: сверяется поле `boundaries`, соответствие прозе issue проверяет чтение |
| Схема одна на оба режима | `grep -rn 'plan-schema.json' .github/workflows .claude/agents docs scripts` | файл схемы ровно один; на него ссылаются `agent-plan.yml` (промпт и `--json-schema`), `docs/rules/plan.md`, `plan.sh`, `plan.test.sh`, а после `2002285` — и `.claude/agents/step-plan.md`. Локальная половина закрыта, прогон повторён оркестратором |
| Решено вслух: «Как устроено сейчас», отметка защищённых путей, лимит размера, самоопределение по типу объекта | комментарий с планом плюс схема и правила | в схеме есть `current_state`, `files[].protected`, `files[].owner`; предупреждение о размере воспроизводится сценарием (20 файлов → предупреждение, код 0); различение по `url` описано в `docs/rules/review-correctness.md` |
| Модельный круг: `step-review-correctness` проходит план на выполняемость | `grep -n 'план' docs/rules/review-correctness.md` | раздел «Предмет ревью — план»: пять пунктов чтения и абзац «Чего этот круг не ловит». После `2002285` различение по `url` стоит и в самом определении шага, `tools` и список команд не тронуты. Живым прогоном круг не проверен: правка определения выводит тип шага из доступных до перезапуска CLI |
| Круг встал между `plan/proposed` и апрувом | `grep -n 'plan/proposed' docs/pipeline.md` | в mermaid ребро от узла публикации идёт в `planreview`, а `plan/approved` достижим только через него; отдельным ребром показан путь «валидатор не пропустил» |
| Обвязка не разъехалась | `bash scripts/wiring.sh`, `bash scripts/rules-sync.sh`, `bash scripts/rules-sync.test.sh`, `bash scripts/guard.test.sh` | `wiring.sh` — код 0, скриптов 15, три новых видны: `plan.sh` и `plan.test.sh` вызванными, `protected-paths.sh` освобождённым; `rules-sync.sh` — код 0; `guard.test.sh` — **пройдено 28, провалено 0, пропущено 0**; `rules-sync.test.sh` — **пройдено 25, провалено 0** |
| Definition of Done в части сборки и тестов | `dotnet build --no-restore -c Release`, `dotnet test --no-build -c Release`, `dotnet format --verify-no-changes --no-restore` | предупреждений 0, ошибок 0; **42 из 42, пропущено 0** — совпадает с `tests/test-count.baseline`, снимок не менялся; стиль без правок. `gitleaks` отдельной командой не запускался — его нет в списке команд шага; отработал `pre-commit` на каждом коммите |
| Триггер планового круга в цикле | `непроверяем` | работы в этом PR нет по решению владельца. Расхождение режимов записано в `docs/pipeline.md`, раздел «Как апрувить план» |

Прогоны сверх контракта:

| Что запускалось | Исход |
|---|---|
| `bash scripts/step-bash-allow.test.sh` | пройдено 56, провалено 0 |
| `bash scripts/test-count-invariant.test.sh` | пройдено 14, провалено 0 |
| `bash scripts/plan.sh validate scripts/fixtures/plans/83.json` без `--at` | код 1, шестнадцать сообщений класса 5 — разница деревьев, а не строгость валидатора |
| `bash scripts/plan.sh validate scripts/fixtures/plans/81.json --at 314016d` с `action: modify` у журнала | код 1 «путь на modify не существует: docs/tasks/81.md» — журнал заводится реализацией, действие исправлено на `create` по измерению, а не по догадке |

Прогоны вне контракта на стадии разбора и планирования:

| Что запускалось | Исход |
|---|---|
| `grep -rniE "шест\|сем[ьи]\|разделов\|[67] раздел"` по `scripts/ docs/ .claude/ .github/` | число разделов плана захардкожено в одном месте — `docs/rules/plan.md:13`; PR #98 третье вхождение в `.claude/agents/step-plan.md:49` уже снял, `scripts/rules-sync.sh` о структуре плана не знает вовсе |
| `gh issue view 98 --json url,number` | `"url": ".../pull/98"` — предложенный в теле задачи различитель issue и PR не работает, `gh issue view` на номере PR успешно возвращает PR |
| `sed -n '190,240p' scripts/rules-sync.sh` | проверка 2 требует упоминания файла правил и промптом, и определением — вывод плана «новый файл правил заводить нельзя» подтверждён |
| `grep` по checkout работ в `.github/workflows/ci-fast.yml` | у «Проверок обвязки» `fetch-depth` не задан, у «Гейта целостности» — `0`; риск 3 плана настоящий |
| `gh variable list` | пусто, `AGENT_LOOP_ENABLED` не установлена — цикл выключен, метки на issue безопасны |

Прогоны оркестраторской части (вынос списка защищённых путей):

| Что запускалось | Исход |
|---|---|
| `bash scripts/guard.test.sh` до выноса | пройдено 28, провалено 0, пропущено 0 |
| `bash scripts/guard.test.sh` после выноса | пройдено 28, провалено 0, пропущено 0 — поведение гейта не изменилось |
| `guard.sh` на копии с пустым `PROTECTED_PATTERNS` | код 2 и «Список защищённых путей пуст». Без этой страховки проверка 1 продолжилась бы с пустым списком и пропускала бы что угодно, оставаясь зелёной |
| `guard.sh` на копии без файла списка | код 2 и «Не найден список защищённых путей» |
| `bash scripts/wiring.sh` | код 0; `protected-paths.sh` освобождён маркером с причиной, два будущих скрипта появятся вызванными |
| `bash scripts/rules-sync.sh` | код 0 после правки двух определений |
| `bash scripts/step-bash-allow.test.sh` | пройдено 56, провалено 0 |

Отдельное наблюдение, не относящееся к задаче: `pre-commit` один раз отклонил
коммит журнала сообщением про находку gitleaks, а дословно та же команда хука
(`gitleaks git --staged --redact --no-banner --log-level warn .`) на том же
индексе дала код 0 и «no leaks found»; повторный коммит прошёл без единой правки
файла. В диффе были только ссылки на комментарии issue. Похоже на случайное
падение, а не на находку; при повторении заводится issue.

## Осталось

Реализация закончена, черновой PR #99 открыт, метка `agent/allow-protected`
поставлена владельцем, все двенадцать проверок зелёные. За рамками задачи не
осталось ничего, кроме названного ниже; следующий шаг — ревью.

**Два пункта контракта приёмки, закрытые наполовину на момент возврата шага,
закрыты полностью:** локальный потребитель схемы — `step-plan.md`, локальный
круг ревью плана — `step-review-correctness.md`. Проверено сверкой
`scripts/rules-sync.sh` (код 0) и её харнессом (25 из 25), а после `c07b105` —
и живым вызовом шага.

**Диагноз «нужна свежая сессия», стоявший здесь раньше, был неверен.** Тип шага
`step-review-correctness` пропал из доступных не из-за кэша сессии, а потому
что определение перестало грузиться вовсе: коммит `2002285` переписал
`description` во фронтматере, и в новой формулировке появилось двоеточие —
`… плана: расхождение …`. Для YAML это вложенное отображение, а не текст
(`mapping values are not allowed here`). Отказ молчаливый: ни харнесс, ни
`rules-sync.sh`, ни `wiring.sh` о нём не сообщают — они сверяют ссылки между
файлами, а фронтматер не парсят. Починка — `c07b105`, скаляр закавычен целиком;
разобраны парсером все семь определений, ломалось ровно одно. Тип шага стал
доступен через ход после правки, без перезапуска CLI.

Отсюда — кандидат на отдельную задачу: `scripts/rules-sync.sh` проверяет
ссылки, но не разбирает фронтматер, поэтому определение, которое не грузится,
проходит все проверки репозитория зелёным. Это ровно тот класс, против которого
написан абзац про непроверенные ссылки в `.claude/CLAUDE.md`.

Не входило и не сделано намеренно: триггер планового круга в агентском цикле —
в `agent-review.yml` нет события на `plan/proposed`, уходит отдельной задачей.

Цикловая половина живым прогоном не проверена: `AGENT_LOOP_ENABLED` не
установлена, цикл выключен. Проверено чтением и локальным прогоном тех же двух
команд, которые вызывает workflow. Записано заранее как ожидаемое место
отказа: `--max-turns 25` у планирования — чтение схемы добавляет ходов.

## Ревью

Оба шага прошли на голове `c07b105`, оба вернули `approve`. Замечания
опубликованы на PR, здесь — только то, чего в комментариях нет.

- корректность — [#issuecomment-5470271818](https://github.com/k-kuzmin/domovoy/pull/99#issuecomment-5470271818): 3 major, 2 nit;
- безопасность — [#issuecomment-5470199578](https://github.com/k-kuzmin/domovoy/pull/99#issuecomment-5470199578): 1 major, 2 nit.

Четыре major, по которым нужно решение владельца:

1. **Рендер не экранирует `|` в значениях** (`scripts/plan.sh:596-601`, `:616-621`,
   `:624-627`). Единственный major с настоящим поведенческим следствием: способ
   проверки вида `grep -nE 'curl|anthropic|claude'` стоит в контракте приёмки
   самой этой задачи, и первый же такой план разъедет таблицу в опубликованном
   комментарии. Инвариант рендера это не ловит — `grep -qF` находит значение и в
   сломанной таблице.
2. **`check_current_state` не мутируется ни одним сценарием**
   (`scripts/plan.test.sh`). Названный путь ложного отказа: `file_line_count`
   считает `wc -l`, и на файле без завершающего перевода строки наблюдение о
   последней строке будет объявлено выходящим за длину файла. Плюс два из
   четырёх подслучаев класса 3 не проверяются.
3. **Доказательство в фикстуре не от того прогона**
   (`scripts/fixtures/plans/83.json:307-308`): `method` описывает мутацию
   инварианта, `evidence` приводит числа мутации гейта. Сверено с
   `docs/tasks/83.md:152` — расхождение реальное. На поведение валидатора не
   влияет, на доверие к фикстуре влияет: её README обещает дословность, а риск 5
   плана назвал дословность мерой против подгонки фикстуры её же автором.
4. **План опознаётся по «последнему комментарию», а не по автору**
   (`.claude/agents/step-review-correctness.md`, `docs/rules/review-correctness.md`).
   Репозиторий публичный; тот же класс угрозы в этом же PR закрыт гейтом
   `author_association` в `agent-plan.yml`, у нового канала чтения плана гейта
   нет. Правка обоих файлов — за оркестратором, не за шагом реализации.

Ревью корректности назвало вслух пункт контракта со значением `непроверяем` —
триггер планового круга в агентском цикле — и отдельно отметило, что
утверждение тела PR «все двенадцать проверок зелёные» на момент первого снимка
было преждевременным: три работы ещё шли. На повторном снимке подтвердилось.

Ссылки на строки в обоих вердиктах выборочно сверены оркестратором до
публикации: `plan.sh:596-601`, `83.json:307-308` против `83.md:152`,
отсутствие `current_state` в харнессе, `step-review-correctness.md:46-48`.
Сошлись все.

## Починка

Красных проверок не было: чинились семь замечаний ревью. Major про опознание
плана по последнему комментарию закрыт до починки оркестратором (`95c8fc4`) —
это конфигурация прав шага, запись 0018.

**Причины, которых не видно в диффе.**

- **Пайп в ячейке.** Разъезжается не рендер, а опубликованная таблица, и
  инвариант «каждое строковое значение доезжает до вывода» этого поймать не
  мог: он ищет значение дословно, а в разъехавшейся таблице оно находится.
  Поэтому сценарий утверждает не наличие текста, а число разделителей в строке
  после выброса экранированных. В саму согласованную фикстуру пайп не положен
  намеренно: там он поссорил бы два сценария — экранирование выглядело бы для
  инварианта как пропажа значения.
- **Ревью назвало семь полей, экранируются девять.** Добавлены `tests[].file`
  (тот же случай, что `path`), `tests[].covers[]` (идёт через `join`, и пайп
  внутри идентификатора разрезал бы ячейку) и `acceptance[].id` — по схеме это
  свободная строка без `pattern`. Половинчатое экранирование вернулось бы тем
  же замечанием следующим кругом.
- **Обратная сторона `covers` проверена на фикстурах до правки, а не после.**
  Контракт приёмки у #81 и #83 восстановлен вручную, и опечатка в
  идентификаторе жила бы именно там; разность множеств `tests[].covers` и
  `acceptance[].id` пуста у всех трёх, путей с ведущим `/` или сегментом `..`
  в них нет. Поэтому красный харнесс после правки означал бы ошибку правки, а
  не находку в фикстуре.
- **`wc -l` и файл без завершающего перевода строки не чинились.** Ревью
  назвало это мотивацией, а не заданием: наблюдение о последней строке такого
  файла валидатор объявит выходящим за длину. Разрыв остаётся, сценария,
  закрепляющего нынешнее поведение как правильное, не написано — иначе
  следующий круг нашёл бы его как новое.
- **`.claude/CLAUDE.md` вне машинной проверки адресата** — назван вслух в
  `docs/rules/plan.md`. Значение уровня «человек» в `enum` поля `owner` не
  заводилось: правка схемы тянет `plan.sh`, `plan.md` и сценарий полноты, то
  есть выходит за границы починки.
- **Рекурсивный обход схемы проверен на себе.** Замена одной потенциально
  вакуумной проверки на другую ничего бы не дала: обход прогоняется по копии
  фикстуры с удалённым `files[].why` и обязан назвать `план.files[].why`.

**Прогоны починки.**

| Что | Исход |
|---|---|
| `bash scripts/plan.test.sh` | пройдено 28, провалено 0, пропущено 0 (было 23) |
| «Проверки обвязки» на голове `3de238c` | тот же харнесс на запушенном дереве — 28, провалено 0; рядом `guard.test.sh` 28, `rules-sync.test.sh` 25, `step-bash-allow.test.sh` 56, `test-count-invariant.test.sh` 14 |
| `bash scripts/guard.test.sh` | пройдено 28, провалено 0, пропущено 0 |
| `bash scripts/rules-sync.sh` | код 0 |
| `bash scripts/wiring.sh` | код 0, скриптов 15 |
| `dotnet build --no-restore -c Release` | предупреждений 0, ошибок 0 |
| `dotnet test --no-build -c Release` | 42 из 42, пропущено 0 |
| `dotnet format --verify-no-changes --no-restore` | без правок |

Порядок прогонов назван потому, что он влияет на то, что чем доказано: первые
локальные 28/0 сняты **до** правки `scripts/fixtures/plans/83.json` и
`docs/rules/plan.md`, а эти два файла читают сценарии исторических планов и
порядка заголовков. Запушенное состояние целиком подтверждено работой
«Проверки обвязки» на голове `3de238c` — тот же харнесс на Linux — и повторным
локальным прогоном на том же дереве.
