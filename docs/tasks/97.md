# Задача #97 — машиночитаемый план и детерминированный валидатор

**Стадия:** план
**Ветка:** —
**PR:** —
**Следующий шаг:** план опубликован
([#issuecomment-5443008699](https://github.com/k-kuzmin/domovoy/issues/97#issuecomment-5443008699)),
метки `agent/ready` и `plan/proposed` стоят — ждём `plan/approved` от владельца.
**После апрува порядок обязателен и не переставляется:** оркестратор правит
`.claude/agents/step-implement.md` и `step-fix.md` (две команды `bash
scripts/plan.sh` и `bash scripts/plan.test.sh` в список хука) → CLI
перезапускается, потому что определения замерзают на старте сессии → только
тогда вызывается `step-implement`. Иначе шаг проведёт сессию, получая отказ
хука на своём же харнессе.

Открыты три хвоста **за человеком**: метка `agent/allow-protected` (без неё
первый же пуш ветки завернёт «Гейт целостности»), правка числа «шесть» в разделе
«Тесты» тела задачи и сам апрув плана. Статус доски не выставлялся ни разу:
`scripts/set-project-status.sh` требует токен со scope `project`, которого у
локальной сессии нет.

Ответы владельца —
[#issuecomment-5442786805](https://github.com/k-kuzmin/domovoy/issues/97#issuecomment-5442786805),
вердикт триажа —
[#issuecomment-5442665634](https://github.com/k-kuzmin/domovoy/issues/97#issuecomment-5442665634).

- [x] разбор — вердикт «не готова», четыре вопроса заданы, ответы получены
- [ ] план — согласован владельцем
- [ ] реализация
- [ ] ревью
- [ ] починка

## Границы

Входит: схема машиночитаемого плана, детерминированный валидатор в `scripts/`
с парным `*.test.sh`, интеграция в оба места публикации плана
(`.github/workflows/agent-plan.yml` и `.claude/agents/step-plan.md`), модельный
круг ревью плана в локальном режиме — `docs/rules/review-correctness.md`,
`.claude/agents/step-review-correctness.md`, `docs/pipeline.md`,
`docs/rules/plan.md`.

Не входит: риск-скоринг, мутационное тестирование, сам советник, триггер
планового круга ревью в цикле (`agent-review.yml` запускается по событиям PR,
события на `plan/proposed` там нет — уходит отдельной задачей).

## Вопросы и ответы

| Вопрос | Ответ | Кто ответил |
|---|---|---|
| Метка `agent/allow-protected` — поставить | За владельцем, агент себе гейт не ставит. На момент записи метки нет | владелец |
| Граница PR: только валидатор или сразу и модельный круг ревью плана | **Один PR.** Размер выше ориентировочных десяти файлов принят осознанно | владелец |
| Искусственных планов шесть или семь — классов нарушений стало семь | **Семь**, по одному на класс, включая различение `непроверяем` и пустой ячейки. Число «шесть» в теле задачи правит владелец | владелец |
| «Реальные планы из истории» как экземпляры схемы — перенос в объёме или критерий `непроверяем` | **Два-три плана переносятся вручную, перенос в объёме.** Вариант «признать `непроверяем`» отвергнут: дыра в критерии дороже трёх файлов ручной работы | владелец |

Пришло готовым из #93 и переспрашиванию не подлежит (комментарий владельца от
25.08): контракт приёмки живёт в плане; `непроверяем` — значение схемы, а не
отсутствие поля; контракт обязателен всегда; контракт проходит ревью
корректности, отдельного шага приёмки нет.

## Решения

**Разбиение на две issue не предлагается.** Владелец отклонил его явно
(комментарий от 26.08) с причиной: разделение труда «что ловит скрипт, а что
остаётся чтению» иначе решается дважды и расходится дырой или дублем. Причина
принята; вопрос владельцу переформулирован на границу PR — разделение труда
решается планом этой задачи один раз, реализация может уехать двумя PR.

**Вопрос про лимит размера плана снят с триажа.** Тело задачи само отдаёт его
планированию. Вердикт шага держал его блокирующим — поправлено: готовности
задачи он не мешает.

**Два расхождения с критерием приёмки добавлены оркестратором.** Шаг их не
нашёл: классов нарушений семь, а не шесть (седьмой — различение `непроверяем`
и пустой ячейки, обязательное с 25.08 и названное хвостом в журнале #93), и
машиночитаемых планов в истории задач не существует ни одного, поэтому вторая
половина критерия невыполнима как записана, а не трудна.

**Правку `.claude/agents/step-plan.md` и `step-review-correctness.md` делает
оркестратор.** `docs/rules/plan.md:25-31` запрещает поручать конфигурацию прав
шага реализации, и запрет покрывает тело определения, не только `tools` и
список команд. Обоснование —
[0018](../decisions/0018-local-orchestrator-and-step-subagents.md).
Планирование добавило к этим двум ещё два определения — `step-implement.md` и
`step-fix.md`, которым нужны в списке хука `bash scripts/plan.sh` и
`bash scripts/plan.test.sh`. Итого четыре правки за оркестратором, и две из них
обязаны лечь до старта реализации.

### Решения планирования (полностью — в комментарии с планом)

**«Как устроено сейчас» получает поле схемы**, а не остаётся прозой рядом:
человекочитаемый комментарий рендерится из структуры, значит раздел без поля
физически не попал бы в опубликованный план.

**Адресат правки становится полем `owner`** (`implement` | `orchestrator`) — это
названное расхождение с формулировкой доработки владельца «скрипт видит путь, не
видит адресата». Классов нарушений остаётся семь: адресат входит в класс 3.

**Лимит размера плана — предупреждение, не отказ**, порог 15 файлов от ориентира
«до 10» в `docs/rules/triage.md:28-30`. Жёсткий предел краснел бы на плане,
который человек уже одобрил.

**Самоопределение `step-review-correctness` по типу объекта принято, но не в
предложенной форме.** Наивный различитель «вызов `gh issue view` удался — значит
issue» отвергнут измерением: `gh issue view 98` успешно возвращает **PR**.
Различение по сегменту `url` (`/pull/` против `/issues/`) — однозначно, команда
шагу уже разрешена.

**Новый файл правил заводить нельзя** — проверка 2 `scripts/rules-sync.sh`
требует упоминания файла правил и промптом `agent-*.yml`, и определением
`step-*.md`, а процесса планового круга в цикле нет. Режим ревью плана живёт
разделом в `docs/rules/review-correctness.md`.

**Список защищённых путей выносится в `scripts/protected-paths.sh`** и
источается `guard.sh` и валидатором: своя копия в валидаторе была бы четвёртым
описанием одного множества.

**Записи в `docs/decisions/` не заводится**: обоснование механических сверок в
этом репозитории живёт в шапке скрипта, как у `wiring.sh` и `rules-sync.sh`.

**Плановый круг ревью к этому плану не применялся.** Механизм — предмет самой
задачи: `docs/rules/review-correctness.md` режима «предмет ревью — план» пока не
содержит, а определение шага говорит про дифф PR. Вызов шага с номером issue
сейчас дал бы поведение, не описанное ни одним правилом. Вместо круга несущие
факты плана перепроверены оркестратором вручную — список в комментарии с планом,
раздел «Сверка оркестратора перед публикацией».

## Изменённые файлы

| Файл | Зачем |
|---|---|
| `docs/tasks/97.md` | этот журнал |

## Проверено

Контракта приёмки ещё нет — плана нет.

| Пункт контракта | Что запускалось | Исход |
|---|---|---|
| — | — | — |

Прогоны вне контракта на стадии разбора и планирования:

| Что запускалось | Исход |
|---|---|
| `grep -rniE "шест\|сем[ьи]\|разделов\|[67] раздел"` по `scripts/ docs/ .claude/ .github/` | число разделов плана захардкожено в одном месте — `docs/rules/plan.md:13`; PR #98 третье вхождение в `.claude/agents/step-plan.md:49` уже снял, `scripts/rules-sync.sh` о структуре плана не знает вовсе |
| `gh issue view 98 --json url,number` | `"url": ".../pull/98"` — предложенный в теле задачи различитель issue и PR не работает, `gh issue view` на номере PR успешно возвращает PR |
| `sed -n '190,240p' scripts/rules-sync.sh` | проверка 2 требует упоминания файла правил и промптом, и определением — вывод плана «новый файл правил заводить нельзя» подтверждён |
| `grep` по checkout работ в `.github/workflows/ci-fast.yml` | у «Проверок обвязки» `fetch-depth` не задан, у «Гейта целостности» — `0`; риск 3 плана настоящий |
| `gh variable list` | пусто, `AGENT_LOOP_ENABLED` не установлена — цикл выключен, метки на issue безопасны |

Отдельное наблюдение, не относящееся к задаче: `pre-commit` один раз отклонил
коммит журнала сообщением про находку gitleaks, а дословно та же команда хука
(`gitleaks git --staged --redact --no-banner --log-level warn .`) на том же
индексе дала код 0 и «no leaks found»; повторный коммит прошёл без единой правки
файла. В диффе были только ссылки на комментарии issue. Похоже на случайное
падение, а не на находку; при повторении заводится issue.

## Осталось

Вся реализация: план опубликован, кода нет ни строки. За человеком три хвоста —
апрув плана (`plan/approved`), метка `agent/allow-protected` и число «шесть» в
разделе «Тесты» тела задачи. Метка нужна до первого пуша ветки, не до апрува.

За оркестратором после апрува, до вызова реализации: правки
`.claude/agents/step-implement.md` и `step-fix.md` и перезапуск CLI.
