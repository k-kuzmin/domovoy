# Задача #109 — критерий границы рамок в правилах реализации, плана и ревью

**Стадия:** реализация завершена, черновой PR открыт
**Ветка:** `feat/109-boundary-criterion-references`
**PR:** #111 — черновик
**Следующий шаг:** попросить у человека метку `agent/allow-protected` на PR #111 (до неё гейт целостности красный) и снятие черновика — снятие запускает ревью

- [x] разбор — границы ясны, вопросы заданы
- [x] план — согласован владельцем (круг 3, ревью плана круг 2 — approve)
- [x] реализация
- [ ] ревью
- [ ] починка

## Границы

Входит:

- `docs/rules/implement.md` — отсылка к разделу «Поведение при
  неопределённости» в пункте «выходить за рамки задачи»
- `docs/rules/plan.md` — та же отсылка в пункте 7 «Что не входит»
- `docs/rules/review-correctness.md` — исправление адреса в абзаце «Какой из
  комментариев считать планом»
- `docs/tasks/109.md` — этот журнал

Не входит:

- `.claude/agents/step-review-correctness.md:57` — дословная копия того же
  неточного утверждения; другой файл и другое выражение, попутное в стороне.
  Заведена #110, правит её оркестратор
- `.claude/CLAUDE.md` — критерий уже на месте (#106 / PR #108)
- `docs/rules/fix.md` — отсылка уже стоит (строки 42–44)
- `scripts/rules-sync.sh` — механической проверки «названа ли граница» задача
  не заводит
- остальные `docs/rules/**` — `triage.md`, `review-security.md`, `reading.md`,
  `README.md`: пересказов критерия и неточных адресов в них нет
- переформатирование соседних строк и любой попутный редакторский проход

## Вопросы и ответы

| Вопрос | Ответ | Кто ответил |
|---|---|---|
| Существует ли шаг с именем, которое уходит в текст правила | `grep -n 'Предмет ревью' .github/workflows/agent-review.yml` — строка 626, `- name: Предмет ревью — план`; job `plan-correctness` на 589, выборка на 644. Риск 7 плана снят, остановки не потребовалось | реализация, прогон в дереве |

## Решения

**Отсылка, а не пересказ.** В `implement.md` и `plan.md` уходит одна фраза с
именем раздела указателя и путём инлайн-кодом — форма как в
`docs/rules/fix.md:42–44`. Продолжение фразы из `fix.md:43` («она про место и
класс дефекта») намеренно не переносится: это был бы пересказ критерия, он
провалил бы второй grep приёмки и попал бы под проверку 6 сверки правил.

**Цитата выборки берётся с конструированием массива.** В
`review-correctness.md` стоит `[.comments[] | select(.authorAssociation ==
"OWNER")] | last` — ровно начало строки `.github/workflows/agent-review.yml:644`.
Без квадратных скобок `last` нечему быть последним, и цитата перестала бы
описывать механизм, на который ссылается абзац. Замечание круга 1 ревью плана.

**Форма отсылки не меняется на markdown-ссылку.** Проверка 5
`scripts/rules-sync.sh:640` собирает только markdown-ссылки и новую отсылку не
видит — механической проверки у неё нет. Это осознанная цена: задача запрещает
менять форму ради того, чтобы её заметил скрипт.

## Изменённые файлы

| Файл | Зачем |
|---|---|
| `docs/rules/implement.md` | пункт «выходить за рамки задачи» больше не обрывается там, где начинается критерий: названо, где искать границу попутного |
| `docs/rules/plan.md` | то же в пункте 7 «Что не входит» — состав плана и правила реализации отсылают в одно место |
| `docs/rules/review-correctness.md` | адрес в абзаце «Какой из комментариев считать планом» приведён к тому, что цикл делает после PR #107: выборка предмета ревью шагом «Предмет ревью — план», а не гейт `author_association` в `agent-plan.yml` |
| `docs/tasks/109.md` | журнал задачи |

## Проверено

| Пункт контракта | Что запускалось | Исход |
|---|---|---|
| reference-not-retelling | `grep -n 'Поведение при неопределённости' docs/rules/implement.md docs/rules/plan.md` | две строки — `docs/rules/implement.md:79` и `docs/rules/plan.md:64`, по одной на файл, якорь целиком внутри одной физической строки. До правки — ноль строк |
| reference-not-retelling | `grep -n 'место и класс\|доводится до конца сейчас' docs/rules/implement.md docs/rules/plan.md` | пустой вывод (код 1). До правки — тоже пусто: единственное «место и класс» в `docs/rules/` лежит в `fix.md:43` |
| address-fixed | `grep -n 'plan-correctness' docs/rules/review-correctness.md` | одна строка — `30:«Предмет ревью — план» в job \`plan-correctness\``, токен целиком внутри строки. До правки — ноль |
| address-fixed | `grep -n 'закрыт проверкой .author_association. в ..github/workflows/agent-plan.yml' docs/rules/review-correctness.md` | пусто. Шаблон прогнан дословно, но состояний он не различает: пусто было и до правки — «закрыт» закрывает строку 29, «проверкой» открывает 30 |
| address-fixed (различающая команда) | `grep -n 'agent-plan.yml' docs/rules/review-correctness.md` | пусто. До правки — одно попадание, строка 30. Именно эта команда отличает исправленный адрес от неисправленного |
| address-fixed (сверка цитаты) | `grep -n 'select(.authorAssociation' docs/rules/review-correctness.md .github/workflows/agent-review.yml` | две строки: `docs/rules/review-correctness.md:32` и `.github/workflows/agent-review.yml:644`. Цитируемое выражение `[.comments[] \| select(.authorAssociation == "OWNER")] \| last` совпадает с началом строки 644; хвост `\| .body // empty` в правило не переносится |
| rules-sync-green | `bash scripts/rules-sync.sh` | код 0 |
| wiring-green | `bash scripts/wiring.sh` | код 0 |

Прогоны сверх контракта:

| Что | Команда | Исход |
|---|---|---|
| сборка | `dotnet build --no-restore -c Release` | 0 предупреждений, 0 ошибок |
| тесты | `dotnet test --no-build -c Release` | 42 из 42, пропущено 0 |
| стиль | `dotnet format --verify-no-changes --no-restore` | код 0, без замечаний |
| поиск секретов | локально не прогонялся: `gitleaks` шагу реализации из Bash не выдан | за CI — обе работы `gitleaks` на PR #111 в состоянии SUCCESS |
| состав диффа | `gh pr view 111 --json files` | четыре пути, ровно границы плана: три `docs/rules/**` и `docs/tasks/109.md` |
| проверки на PR #111 | `gh pr view 111 --json statusCheckRollup` | 8 SUCCESS, 2 SKIPPED (тяжёлые не нужны), 1 FAILURE — «Гейт целостности» без метки `agent/allow-protected` на PR |

## Осталось

- метка `agent/allow-protected` на PR — ставит человек; до неё гейт целостности
  красный, потому что три из четырёх файлов лежат под `docs/rules/.+`
  (`scripts/protected-paths.sh:57`). Это ожидаемое состояние, а не падение
- `.claude/agents/step-review-correctness.md:57` — та же неточность в
  определении локального шага; вынесена в #110, правит оркестратор
