# Задача #109 — критерий границы рамок в правилах реализации, плана и ревью

**Стадия:** круг 1 ревью пройден — обе половины `approve`; замечание Copilot разобрано и отклонено. Нужен ли круг 2 — решение владельца, не принято
**Ветка:** `feat/109-boundary-criterion-references`
**PR:** #111 — черновик снят человеком 03.09.2026, метка `agent/allow-protected` стоит, все 13 проверок зелёные на актуальном коммите (снятие черновика включило и тяжёлые)
**Следующий шаг:** мерж — действие человека по умолчанию. Открыто и ждёт владельца: нужен ли круг 2 — правок ни круг 1, ни разбор Copilot не породили, дифф с момента круга 1 не изменился, и круг 2 читал бы тот же текст

- [x] разбор — границы ясны, вопросы заданы
- [x] план — согласован владельцем (круг 3, ревью плана круг 2 — approve)
- [x] реализация
- [x] ревью — круг 1: корректность `approve`, безопасность `approve`; замечаний ни одного уровня
- [ ] починка

## Границы

Входит:

- `docs/rules/implement.md` — отсылка к разделу «Поведение при
  неопределённости» в пункте «выходить за рамки задачи»
- `docs/rules/plan.md` — та же отсылка в пункте 7 «Что не входит»
- `docs/rules/review-correctness.md` — исправление адреса в абзаце «Какой из
  комментариев считать планом»
- `docs/tasks/109.md` — этот журнал

Не входит:

- `.claude/agents/step-review-correctness.md:57` — дословная копия того же
  неточного утверждения; другой файл и другое выражение, попутное в стороне.
  Заведена #110, правит её оркестратор
- `.claude/CLAUDE.md` — критерий уже на месте (#106 / PR #108)
- `docs/rules/fix.md` — отсылка уже стоит (строки 42–44)
- `scripts/rules-sync.sh` — механической проверки «названа ли граница» задача
  не заводит
- остальные `docs/rules/**` — `triage.md`, `review-security.md`, `reading.md`,
  `README.md`: пересказов критерия и неточных адресов в них нет
- переформатирование соседних строк и любой попутный редакторский проход

## Вопросы и ответы

| Вопрос | Ответ | Кто ответил |
|---|---|---|
| Существует ли шаг с именем, которое уходит в текст правила | `grep -n 'Предмет ревью' .github/workflows/agent-review.yml` — строка 626, `- name: Предмет ревью — план`; job `plan-correctness` на 589, выборка на 644. Риск 7 плана снят, остановки не потребовалось | реализация, прогон в дереве |

## Решения

**Отсылка, а не пересказ.** В `implement.md` и `plan.md` уходит одна фраза с
именем раздела указателя и путём инлайн-кодом — форма как в
`docs/rules/fix.md:42–44`. Продолжение фразы из `fix.md:43` («она про место и
класс дефекта») намеренно не переносится: это был бы пересказ критерия, он
провалил бы второй grep приёмки и попал бы под проверку 6 сверки правил.

**Цитата выборки берётся с конструированием массива.** В
`review-correctness.md` стоит `[.comments[] | select(.authorAssociation ==
"OWNER")] | last` — ровно начало строки `.github/workflows/agent-review.yml:644`.
Без квадратных скобок `last` нечему быть последним, и цитата перестала бы
описывать механизм, на который ссылается абзац. Замечание круга 1 ревью плана.

**Форма отсылки не меняется на markdown-ссылку.** Проверка 5
`scripts/rules-sync.sh:640` собирает только markdown-ссылки и новую отсылку не
видит — механической проверки у неё нет. Это осознанная цена: задача запрещает
менять форму ради того, чтобы её заметил скрипт.

**Критерий приёмки исправлен в теле задачи, а не обойдён.** Второй
отрицательный шаблон третьего пункта — `закрыт проверкой .author_association. в
..github/workflows/agent-plan.yml` — пуст и на неправленом дереве: «закрыт»
закрывает строку 29, «проверкой» открывает 30. Выполнить его буквально можно
было, ничего не сделав. План этот путь отверг прямо — «критерий принадлежит
владельцу», — поэтому реализация прогнала шаблон дословно и назвала рядом
различающую команду, а решение принял владелец: 03.09.2026 шаблон заменён в
теле #109 на `grep -n 'agent-plan.yml' docs/rules/review-correctness.md` (до
правки одно попадание, после — ноль). Правка сделана после реализации и до
ревью PR #111; дифф от неё не меняется.

### Ревью, круг 1 — записал оркестратор

**Корректность — `approve`, замечаний нет.** Ценное не в вердикте, а в том,
как он получен. Контракт приёмки ревьюер прогнал сам на дереве и опёрся на
**независимый** источник там, где своего прогона не имел: `rules-sync.sh` и
`wiring.sh` шагу ревью из Bash не выданы, поэтому зелёными они признаны по
прогону CI 33723467037, а не по таблице «Проверено» этого журнала — она
утверждение автора о собственной работе. Заодно ревью доказало, что одного
чека хватает на оба пункта контракта: оба скрипта вызываются внутри одного
job `rules` (`ci-fast.yml:212` и `:261`, границы job — 183–286).

Ревью само назвало вслух неразличающий отрицательный шаблон — то же, что
нашли реализация и оркестратор, — и отдельно проверило, что критерий заменён
владельцем, а не переписан агентом.

Проверено сверх контракта: формулировки трёх правок совпадают с
продиктованными планом дословно, включая отступы (два пробела в
`implement.md`, три в `plan.md`); состав диффа — ровно четыре пути границ
плана; признаков имитации починки нет — подавлений, `Skip`,
`continue-on-error` и правок скриптов в диффе нет вовсе. Оставшаяся копия в
`.claude/agents/step-review-correctness.md:57` замечанием не признана:
расхождение описано в плане, теле PR и журнале и заведено как #110.

Расширенная проверка ревью: неточного утверждения нет и в `docs/pipeline.md` —
строки 348–349 говорят «авторство задачи владельцем», что верно.

**Безопасность — `approve`, замечаний нет.** Ревью не отчиталось «чисто» по
неприменимым пунктам, а назвало их неприменимыми: C#-кода в диффе ноль, менять
нечего ни в пути к Home Assistant, ни в белом списке `call_service`, ни в
аутентификации эндпоинтов. Содержательно проверен один абзац — переписанный
«Какой из комментариев считать планом»: он описывает защиту от подделки
комментария-плана посторонним в публичной задаче, и правка **заменяет неверную
атрибуцию защиты на настоящую**. Изложение усилено, не ослаблено; нормативное
требование к локальному шагу сохранено в прежней силе.

Журнал прочитан целиком: реальных `entity_id`, IP, MAC, доменов, топика ntfy,
токенов и ключей нет.

**Нужен ли круг 2 — решает владелец, вопрос задан 03.09.2026.** Факт, на
котором вопрос стоит: круг 1 не породил ни одной правки, дифф после него не
изменился, и круг 2 читал бы ровно тот же текст. Правила «не запускать
холостой круг» в `docs/rules/**` пока нет — она в открытой #92, поэтому
оркестратор такое решение сам не принимает. Потолок записи 0016 не
израсходован: оба круга остаются на случай замечаний Copilot после снятия
черновика.

### Замечание Copilot — разобрано, отклонено, правки нет

Одно замечание уровня «changes recommended»: добавить `:644` к ссылке на
`agent-review.yml` в `docs/rules/review-correctness.md:31`.

**Отклонено, и это не вкусовщина — обоснование проверяемое.**

Замечание опирается на прочтение тела PR, которого там нет: фраза «цитата
приведена в форме `agent-review.yml:644`» адресована читателю PR и говорит,
где в коде лежит цитируемое выражение. Обещания, что номер строки появится в
тексте правила, в ней нет.

По существу: во всём каталоге правил нет **ни одной** ссылки вида
`файл:строка` — `grep -rnE ':[0-9]{1,4}' docs/rules/` (за вычетом
идентификаторов ТЗ) пуст. Принятая форма — путь инлайн-кодом, и задача
ссылается на неё прямо. Взять замечание значило бы завести в правилах форму,
которой в них нет нигде.

**Номер строки — более хрупкое издание того самого дефекта, который этот PR
чинит.** Абзац правился потому, что нёс утверждение о workflow, разошедшееся
с кодом после PR #107. Номер строки разъезжается от любой правки
`agent-review.yml` выше 644-й и не ломает при этом ничего проверяемого:
`scripts/rules-sync.sh` номеров строк не сверяет — проверка 5 видит только
markdown-ссылки, проверка 6 сравнивает нормализованный текст. Прецедент в
проекте уже есть: журнал #106 приведён к сверке цитаты без номеров строк.

**Задача, ради которой номер предлагается, закрыта средствами строже него.**
Абзац несёт четыре независимых якоря, каждый находится одним `grep` и
переживает правки workflow: имя job `plan-correctness`, имя шага «Предмет
ревью — план», путь файла и сама выборка дословно. Последний сверяет
содержание, а не адрес, и расхождение с кодом обнаруживает — тогда как верный
номер строки при изменившемся выражении молча указывал бы на другое.

Сверх того текст этого предложения задан согласованным планом дословно и
входит в критерий приёмки.

Разбор опубликован комментарием к PR и ответом в самой ветке замечания.

### Два прогона `agent-fix`, о которые легко споткнуться

Ревью Copilot — событие `pull_request_review` — завело workflow `agent-fix`
дважды. Ни один не требует действий и ни один не является чеком PR:
в `gh pr checks 111` их нет.

- прогон с `action_required` и **нулём job'ов** — GitHub загейтил запуск,
  инициированный ботом, и он не начинался вовсе;
- следующий прогон — `skipped` на job «Починка ветки задачи»: сработал
  выключатель цикла `vars.AGENT_LOOP_ENABLED == 'true'`
  (`.github/workflows/agent-fix.yml:87`). Основной режим — задачу ведёт человек
  из Claude CLI, и плечи цикла лежат выключенными.

То есть починка Copilot-замечания и не должна была уйти в цикл: разбор сделан
локально, и замечание отклонено. Запись здесь только потому, что `action_required`
в списке прогонов читается как «ждёт моего действия», а он инертен.

## Изменённые файлы

| Файл | Зачем |
|---|---|
| `docs/rules/implement.md` | пункт «выходить за рамки задачи» больше не обрывается там, где начинается критерий: названо, где искать границу попутного |
| `docs/rules/plan.md` | то же в пункте 7 «Что не входит» — состав плана и правила реализации отсылают в одно место |
| `docs/rules/review-correctness.md` | адрес в абзаце «Какой из комментариев считать планом» приведён к тому, что цикл делает после PR #107: выборка предмета ревью шагом «Предмет ревью — план», а не гейт `author_association` в `agent-plan.yml` |
| `docs/tasks/109.md` | журнал задачи |

## Проверено

| Пункт контракта | Что запускалось | Исход |
|---|---|---|
| reference-not-retelling | `grep -n 'Поведение при неопределённости' docs/rules/implement.md docs/rules/plan.md` | две строки — `docs/rules/implement.md:79` и `docs/rules/plan.md:64`, по одной на файл, якорь целиком внутри одной физической строки. До правки — ноль строк |
| reference-not-retelling | `grep -n 'место и класс\|доводится до конца сейчас' docs/rules/implement.md docs/rules/plan.md` | пустой вывод (код 1). До правки — тоже пусто: единственное «место и класс» в `docs/rules/` лежит в `fix.md:43` |
| address-fixed | `grep -n 'plan-correctness' docs/rules/review-correctness.md` | одна строка — `30:«Предмет ревью — план» в job \`plan-correctness\``, токен целиком внутри строки. До правки — ноль |
| address-fixed | `grep -n 'закрыт проверкой .author_association. в ..github/workflows/agent-plan.yml' docs/rules/review-correctness.md` | пусто. Шаблон прогнан дословно, но состояний он не различает: пусто было и до правки — «закрыт» закрывает строку 29, «проверкой» открывает 30. Решением владельца от 03.09.2026 заменён в теле задачи различающей командой из строки ниже |
| address-fixed (различающая команда) | `grep -n 'agent-plan.yml' docs/rules/review-correctness.md` | пусто. До правки — одно попадание, строка 30. Именно эта команда отличает исправленный адрес от неисправленного |
| address-fixed (сверка цитаты) | `grep -n 'select(.authorAssociation' docs/rules/review-correctness.md .github/workflows/agent-review.yml` | две строки: `docs/rules/review-correctness.md:32` и `.github/workflows/agent-review.yml:644`. Цитируемое выражение `[.comments[] \| select(.authorAssociation == "OWNER")] \| last` совпадает с началом строки 644; хвост `\| .body // empty` в правило не переносится |
| rules-sync-green | `bash scripts/rules-sync.sh` | код 0 |
| wiring-green | `bash scripts/wiring.sh` | код 0 |

Прогоны сверх контракта:

| Что | Команда | Исход |
|---|---|---|
| сборка | `dotnet build --no-restore -c Release` | 0 предупреждений, 0 ошибок |
| тесты | `dotnet test --no-build -c Release` | 42 из 42, пропущено 0 |
| стиль | `dotnet format --verify-no-changes --no-restore` | код 0, без замечаний |
| поиск секретов | локально не прогонялся: `gitleaks` шагу реализации из Bash не выдан | за CI — обе работы `gitleaks` на PR #111 в состоянии SUCCESS |
| состав диффа | `gh pr view 111 --json files` | четыре пути, ровно границы плана: три `docs/rules/**` и `docs/tasks/109.md` |
| проверки на PR #111 | `gh pr view 111 --json statusCheckRollup` | 8 SUCCESS, 2 SKIPPED (тяжёлые не нужны), 1 FAILURE — «Гейт целостности» без метки `agent/allow-protected` на PR |

## Осталось

- **мерж** — по умолчанию действие человека
- ~~снять черновик~~ — сделал человек 03.09.2026, запись 0015. Снятие
  запустило ревью Copilot: одно замечание, отклонено с обоснованием, правки
  из него не вышло
- ~~метка `agent/allow-protected` на PR~~ — поставлена человеком 03.09.2026.
  Гейту хватило коммита журнала как нового события: `rerun` его не
  перезапускает, но close+reopen не понадобился
- `.claude/agents/step-review-correctness.md:57` — та же неточность в
  определении локального шага; вынесена в #110, правит оркестратор
