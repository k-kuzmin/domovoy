# Задача #84 — похожесть и цена перед заведением задачи, служебный агент выжимки

**Стадия:** реализация
**Ветка:** `feat/84-issue-scout-before-create`
**PR:** черновой, открыт по готовности реализации
**Следующий шаг:** оркестратору — три живых прогона `issue-scout` в новой
сессии (пункты `live-similar`, `live-price`, `live-trifle` контракта), вывод
комментарием в #84 и в раздел «Проверено»; после этого черновик с PR снимает
человек

- [x] разбор — границы ясны, вопросы заданы
- [x] план — согласован владельцем (круг 3, финальный)
- [x] реализация — восемь файлов шага; два файла оркестратора легли раньше
- [ ] ревью
- [ ] починка

## Границы

Входит — десять файлов из раздела «Файлы» согласованного плана:

- `.claude/CLAUDE.md` — раздел «Перед заведением задачи — похожесть и цена»,
  правка абзаца о попутной находке, оговорки про класс служебных агентов;
- `.claude/agents/issue-scout.md` — определение служебного субагента выжимки;
- `scripts/rules-sync.sh`, `scripts/rules-sync.test.sh` — два класса
  определений вместо одного;
- `docs/rules/README.md`, `docs/rules/plan.md`, `docs/pipeline.md` — места, где
  класс определений локального режима назван шаблоном `step-*.md`;
- `docs/decisions/0021-service-agents-alongside-steps.md` и строка в
  `docs/decisions/README.md`;
- этот журнал.

Первые два файла правит оркестратор (план, столбец «Кто правит»), остальные —
шаг реализации.

Не входит: автоматическое закрытие дубликатов, критерии триажа, объединение
#75 и #82, PR на несколько задач, четвёртый сборщик `risk-score.sh` по тексту
задачи, промпты цикла (`gh issue create` не встречается ни в одном
`agent-*.yml`), механическая проверка отсутствия `Edit` и `Write` в `tools`,
определения субагента проверки поведением (#94) и советника фазы 4, правка
текста записи 0018.

## Вопросы и ответы

| Вопрос | Ответ | Кто ответил |
|---|---|---|
| Где живёт правило — `docs/rules/` или `.claude/CLAUDE.md` | `.claude/CLAUDE.md`: файл в `docs/rules/`, к имени которого не сходится ни один шаг, сверка считает общим правилом и требует ссылку у всех потребителей — шаги задач не заводят | план, круг 1 |
| Имя и видимость определения | `issue-scout.md` без префикса `step-`, сверка расширяется на весь каталог | план, круг 1 |
| Модель служебного агента | `haiku`, проверяется живыми прогонами, а не назначается | план, круг 1 |
| Порог «дубликат» и порог «мелочь» | вердикт модели с явно названным критерием; перцентили корпуса — ориентир в теле определения | план, круг 1 |
| Признак сборной, порог разряда, потолок пачки | общее место и общий класс; пять пунктов или один срочный; десять файлов по `risk-score.sh diff` | план, круг 1 |
| Нужно ли то же в цикле | нет: `gh issue create` не встречается ни в одном `.github/workflows/agent-*.yml` | план, круг 1 |

## Решения

**Класс служебных агентов выводится из имени, а не назначается вторым
префиксом.** `.claude/agents/*.md` делится надвое: `step-*.md` — шаги,
остальное — служебные агенты. Отвергнут префикс `service-*.md`: он
воспроизводит ту же дыру, из-за которой задача и заведена, — файл, не
подпавший ни под один префикс, снова не проверяется никем.

**Восемь мест использования массива определений распределены поимённо, а не
формулой.** Три из них остаются только на шагах, и у каждого своя причина:
пустой список служебных агентов законен (`:186`); правило, упомянутое только
служебным агентом, обязано и дальше краснеть (`:258`); множество общих правил
выводится по шагам (`:306`) — иначе служебное имя вида `reading-scout` заберёт
`reading.md` через ветку `:225`, и требование общего правила молча снимется со
всех потребителей. Последнее закрыто отдельным сценарием харнесса.

**Запись [0021](../decisions/0021-service-agents-alongside-steps.md)** кладётся
поверх 0018, а не правит её: записи решений не переписываются
(`docs/decisions/README.md:16`). Числа в 0018 («определений семь на шесть
шагов», «модель названа в двух из семи») остаются состоянием на момент той
записи.

## Изменённые файлы

| Файл | Зачем |
|---|---|
| `.claude/CLAUDE.md` | раздел «Перед заведением задачи — похожесть и цена», три адреса у попутной находки, класс служебных агентов у счёта определений, у поля `model` и в таблице синхронизации. **Правил оркестратор** (коммит `d349d96`) |
| `.claude/agents/issue-scout.md` | определение служебного субагента выжимки. **Правил оркестратор** (коммит `d349d96`) |
| `scripts/rules-sync.test.sh` | восемь сценариев на второй класс определений и на регрессию первого; служебное определение в базовой фикстуре; помощник `expect_absent` — «сверка промолчала об этом» кодом возврата не выражается |
| `scripts/rules-sync.sh` | каталог определений делится на два класса, восемь точек использования массива распределены поимённо, шапка печатает служебных отдельным числом |
| `docs/rules/README.md` | потребители общего правила названы не шаблоном `step-*.md`, а каталогом; абзац про второй класс со ссылкой на 0021 |
| `docs/rules/plan.md` | запрет поручать плану конфигурацию прав субагента сформулирован по каталогу — как в `scripts/plan.sh:176`, а не по шаблону `step-*` |
| `docs/pipeline.md` | таблица слоёв и абзац про локальный режим знают о втором классе |
| `docs/decisions/0021-service-agents-alongside-steps.md` | класс служебных агентов поверх 0018: чем отличается от шага, что сверка требует с каждого класса, почему нет второго префикса |
| `docs/decisions/README.md` | строка 0021 в индексе: запись без строки в таблице не находится |
| `docs/tasks/84.md` | журнал задачи |

## Проверено

| Пункт контракта | Что запускалось | Исход |
|---|---|---|
| **rule-single-source** | `bash scripts/rules-sync.sh`; `grep -rn 'сборн'` и `grep -rn 'похожест'` по `.claude/CLAUDE.md`, `docs/rules/`, `.claude/agents/` | код 0, расхождений 0 (проверка 6 копий не нашла). В `docs/rules/` оба якоря не встречаются ни разу; формулировка правила одна — `.claude/CLAUDE.md:156` и далее, а `.claude/agents/issue-scout.md:23` на этот раздел ссылается по имени |
| **stray-finding-addresses** | `grep -n 'сборн' .claude/CLAUDE.md` | три попадания: 180, 191, 654. Строка 654 лежит в разделе «Поведение при неопределённости»: «Адресов три: дописать в существующую, положить в сборную, завести новую. Какой из них — отвечает раздел „Перед заведением задачи — похожесть и цена“; чего в этом списке нет, так это „забыть“» |
| **definition-created** | `grep -n 'tools:'` и `grep -n 'step-bash-allow'` по `.claude/agents/issue-scout.md`, затем `bash scripts/rules-sync.sh` | `issue-scout.md:6` — `tools: Read, Glob, Grep, Bash`, ни `Edit`, ни `Write`; `issue-scout.md:13` — вызов `scripts/step-bash-allow.sh` аргументами хука `PreToolUse`; сверка — код 0 |
| **sync-sees-definition** | `bash scripts/rules-sync.sh`, `bash scripts/rules-sync.test.sh` | в отчёте `служебные агенты: 1`; харнесс — пройдено 43, провалено 0. Среди пройденных есть сценарий со служебным определением, у которого `Bash` без границы команд (код 1), и сценарий с битой ссылкой `docs/rules/scout.md` из служебного определения (код 1) |
| **trap-named** | `grep -n 'по одному слову' .claude/agents/issue-scout.md` | строка 57: «`gh search issues --repo <репозиторий> "<одно слово>"` — **по одному слову за вызов**. Многословный запрос из редких русских слов молча возвращает пустоту» |
| **live-similar** | не запускалось шагом реализации | закрывает оркестратор: `Agent` у шагов отнят, субагент субагента не порождает (риск 7 плана). Прогон идёт в сессии, начатой после появления `issue-scout.md` в дереве (риск 8) |
| **live-price** | не запускалось шагом реализации | то же |
| **live-price-justified** | помечен в плане как непроверяемый | решает владелец по опубликованной выжимке |
| **live-trifle** | не запускалось шагом реализации | то же, что `live-similar` |

Прогоны вне контракта:

| Что | Исход |
|---|---|
| `dotnet build --no-restore -c Release` | предупреждений 0, ошибок 0 |
| `dotnet test --no-build -c Release` | 42 из 42, пропущено 0 |
| `dotnet format --verify-no-changes --no-restore` | код 0 |
| `bash scripts/rules-sync.test.sh` **до** правки скрипта | пройдено 39, провалено 4 — ровно четыре новых сценария на поведение, которого у сверки ещё не было; четыре сценария-сторожа зелёные и до, и после |

## Осталось

**Три живых прогона выжимки и вердикт по адресу** — `live-similar`,
`live-price`, `live-trifle` и `live-price-justified`. Шаг реализации выполнить
их не может: инструмент `Agent` у шагов отнят, субагент субагента не
порождает. Их запускает оркестратор, вывод едет комментарием в #84 и в таблицу
выше. Прогон обязан идти в сессии, начатой после появления
`.claude/agents/issue-scout.md` в дереве: определения замерзают на старте
сессии, и «такого агента нет» — не отрицательный результат модели.

**Модель `haiku` в определении назначена, но не подтверждена.** Риск 9 плана:
если тремя прогонами она отбор не вытянет, поле `model` снимается (тогда
работает модель сессии оркестратора), а абзац про `model` в указателе
правится под фактический выбор. Это правка защищённого файла — её делает
оркестратор.

**Правило может переехать.** Риск 11: развилка про место правил оркестратора
задана и в #85, где открыто предложение `docs/rules/orchestrator.md`. Здесь
правило легло в `.claude/CLAUDE.md` по механической причине — файл в
`docs/rules/`, к имени которого не сходится ни один шаг, сверка считает общим
правилом и требует ссылку у всех потребителей. Если #85 заведёт файл правил
оркестратора, правило переезжает туда отдельной задачей.
